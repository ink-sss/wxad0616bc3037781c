# Live Room Migration Coverage

Task: `.trellis/tasks/05-30-migrate-h5-live-to-mini-program`

Source of truth: `/Users/apple/Desktop/code/live_h5/src/pages/broadcast/`

## 2026-06-02 Pixel 1:1 Migration Correction

This pass replaces the earlier consolidated/imitation implementation with a direct source-structure migration from `live_h5/src/pages/broadcast/`.

Latest state:

- Copied H5 broadcast runtime files into `uniapp-src/src/pages/broadcast/`: 77 source runtime files from H5 are present. The mini side has one extra adapter file, `utils/domain-ready.js`, plus a compatible no-op `utils/domain-prefetch.js`.
- Copied H5 live-dependent shared components into `uniapp-src/src/components/`: share, center, product buy/list/card, address panels, live sign-in, live report, comment prize modal, coupon selector, countdown and mini-window components.
- Copied `live_h5/src/pages/lottery-preview/` runtime into `uniapp-src/src/pages/lottery-preview/`.
- Copied required H5 static assets into `uniapp-src/src/static`, component-local `static/`, broadcast-local `static/`, lottery-preview-local `static/`, and `uniapp-src/src/assets` where H5 imports required them.
- Registered route coverage is OK for:
  `/pages/broadcast/entry`, `/pages/broadcast/replay`, `/pages/live/live-vertical`, `/pages/live/live-horizontal`, `/pages/center/index`, `/pages/order/confirm`, `/pages/order/list`, `/pages/order/detail`, `/pages/order/pay`, `/pages/order/receipt`, `/pages/address/index`, `/pages/address/edit`, `/pages/report/index`, `/pages/prize-record/index`, `/pages/invitation-record/index`.

Runtime adaptations made in this correction:

- `LivePortraitStage.vue` and `LiveLandscapeStage.vue` keep H5 class names and layout, but render mini `live-player` for live RTMP/FLV sources and mini `video` for replay/video sources.
- `useLivePlayerInitializer.js` is a mini adapter: it writes H5-selected source state, creates mini media contexts, preserves replay ended/auto-next logic, and no longer creates H5 DOM/HLS/FLS players.
- `useLiveVideoRuntime.js`, `useIOSWechatBridgeAutoPlay.js`, `useLiveScreenWakeLock.js`, `useLiveSubscribePush.js`, `useLiveChatInput.js`, `useLivePlaybackDebug.js`, `useLivePageLeave.js`, `useLiveEntryBootstrap.js`, `entry-init.js`, `entry-route.js`, and domain preconnect helpers are mini-safe adapters/no-ops where the H5 primitive has no mini equivalent.
- `useLivePurchase.js` and `useLiveSidePanels.js` no longer import `@dcloudio/uni-h5`; they use the mini `uni` runtime.
- WebSocket remains routed through the mini `MiniLiveSocket` adapter and H5 IM SDK remains unsupported in mini.

Unsupported or intentionally adapted platform items:

- H5 DOM media runtime, HLS/FLV JS players, and Agora/FLS DOM insertion: replaced by mini `live-player`/`video`; no DOM insertion is allowed in mini.
- H5 JSSDK share/address/subscribe flows: replaced by mini share menu, mini address/payment adapters, and `requestSubscribeMessage` where a mini template id exists.
- H5 landing-domain URL rewrite/preconnect: no Mini Program page-code equivalent; domain allowlists must be configured in WeChat.
- Static scan false positives kept intentionally:
  - `slot-machine-window` is an H5 lottery CSS class, not a browser global.
  - `windowHeight` is a WeChat system-info field, not a browser global.
- RTC response fields are retained only as diagnostic/source metadata. Mini playback is not considered ready unless the backend also returns a Mini Program-playable pull URL such as RTMP/FLV/HLS/MP4.

Verification from this pass:

- `npm run build:mp-weixin` passes.
- Generated mini package scan has no executable `window`, `document`, `localStorage`, `sessionStorage`, `navigator`, `hls.js`, `flv.js`, `weixin-js-sdk`, `@dcloudio/uni-h5`, DOM creation/query APIs, `Blob`, `fetch`, or Agora DOM runtime usage in the broadcast implementation. Remaining scan hits are the false positives listed above.

Not completed by automation:

- Pixel comparison screenshots across live portrait, live landscape, replay portrait, replay landscape still require running the H5 baseline and importing the generated mini package into WeChat DevTools/device.
- Real playback can only be declared after testing with a valid live room/recording room and configured WeChat request/socket/upload/download/live-player domains.

Mini implementation surface:

- `uniapp-src/src/pages/broadcast/entry.vue`
- `uniapp-src/src/pages/broadcast/replay.vue`
- `uniapp-src/src/pages/broadcast/useReplayProductSchedule.js`
- `uniapp-src/src/api/live.js`
- `uniapp-src/src/api/marketing.js`
- `uniapp-src/src/api/h5-live.js`
- `uniapp-src/src/utils/live-route.js`
- `uniapp-src/src/utils/mini-live-socket.js`
- `uniapp-src/src/utils/ws-envelope.js`
- `uniapp-src/src/services/h5-auth-context.js`

Open audience entries: none.

## Audience Capability Coverage

| Capability | Status | Mini implementation |
|---|---|---|
| Live playback visual states | adapted | `entry.vue` uses `live-player`, portrait/landscape branches, source failover, poster and ended overlays. |
| Replay playback visual states | adapted | `entry.vue` uses `video`, replay list, poster fade, progress restore/reporting, auto-next replay. |
| Entry overlay and manual sound/play intent | adapted | `entry.vue` `showEntryOverlay`, `enterLiveByGesture`, `manualPlayVideo`, `broadcast_sound_intent` storage. |
| Ended/access-denied/viewer-limit states | adapted | `entry.vue` explicit `live-ended-overlay`, `access-denied-overlay`, `viewer-limit` and enter-room error handling. |
| Marquee ad | adapted | `entry.vue` `shouldShowMarquee`, position classes, dismiss, setting update reset. |
| Chat input, quick replies, mute states | adapted | `entry.vue` quick reply bar, send path, `chatDisabled`, mute-all and user mute socket events. |
| Product shelf/current product | adapted | `entry.vue` product sheet, current teaching card, product detail navigation, buy reminder API. |
| Replay product schedule | ported | `useReplayProductSchedule.js`, called from `entry.vue` replay time updates. |
| Buying notices and replay simulated orders | adapted | `entry.vue` transient buying notice, chat system notice, `getReplaySimMessages` order payload routing. |
| Comments, pinned/delete/top/clear/filter | adapted | `entry.vue` history load, pinned bar, socket delete/top/clear/mute-word handling. |
| Likes and viewer metrics | adapted | `entry.vue` local like feedback, API/socket send, live status/viewer count updates. |
| Normal lottery | adapted | `entry.vue` marketing action list/panel, participant lookup, socket notice refresh. |
| Comment lottery | adapted | `entry.vue` external lottery entry, keyword bubble, comment reward claim, socket notice refresh. |
| Watch rewards | adapted | `entry.vue` watch reward entry, claim API, watch seconds heartbeat state, socket notice refresh. |
| Sign-in | adapted | `entry.vue` sign config, check status, submit panel/tab. |
| Report/share/center/return-to-live | adapted | `entry.vue` complaint navigation, Mini Program share hooks, center navigation, replay return. |
| Replay progress | adapted | `entry.vue` local progress storage and `/h5/live/reportViewProgress`. |
| Mini-window/collapse behavior | adapted | `entry.vue` landscape collapse/restore/hide controls. |
| Adaptive stream selection/status updates | adapted | `live-route.js` candidate ordering, `entry.vue` source failover and live status snapshot switching. |
| Heartbeat/enter/leave | adapted | `entry.vue` `/h5/live/enter`, `/heartbeat`, `/leave`, viewer-limit handling. |
| Socket envelope normalization | ported | `mini-live-socket.js` plus `ws-envelope.js` HMAC wrapper/unwrapper and event normalization. |
| Playback debug float | adapted | `entry.vue` query-gated `debug=1` mini debug panel. |
| Wake lock | adapted | `entry.vue` uses `uni.setKeepScreenOn` on load/unload. |

## File Mapping

| H5 file | Status | Mini mapping / reason |
|---|---|---|
| `entry.vue` | adapted | Consolidated into `uniapp-src/src/pages/broadcast/entry.vue` with mini-native playback, socket, auth, product, comment, marketing, and state UI. |
| `replay.vue` | adapted | `uniapp-src/src/pages/broadcast/replay.vue` normalizes replay query and redirects to entry replay mode. |
| `useReplayProductSchedule.js` | ported | `uniapp-src/src/pages/broadcast/useReplayProductSchedule.js`. |
| `components/LiveBroadcastStageHost.vue` | adapted | Stage host and state branching are in `entry.vue`. |
| `components/LivePortraitStage.vue` | adapted | Portrait live/replay branch, overlays, chat, product, toolbar in `entry.vue`. |
| `components/LiveLandscapeStage.vue` | adapted | Landscape branch, tabs, mini-window collapse, product/sign/interact panels in `entry.vue`. |
| `components/LiveChatBar.vue` | adapted | Input, send, quick replies, mute state, toolbar actions in `entry.vue`. |
| `components/LiveProductShelf.vue` | adapted | Product shelf popup and current product card in `entry.vue`. |
| `components/LiveBroadcastMarketingLayer.vue` | adapted | Mini marketing sheet/entries/notices in `entry.vue`; H5-only modal visuals replaced with mini-safe panels. |
| `components/LiveEntryOverlay.vue` | adapted | `entry.vue` entry overlay and manual play/sound gesture. |
| `components/LiveEndedOverlay.vue` | adapted | `entry.vue` ended overlay. |
| `components/LiveAccessDenied.vue` | adapted | `entry.vue` access denied overlay and UID copy. |
| `components/LiveViewerLimitReached.vue` | adapted | `entry.vue` viewer-limit overlay. |
| `components/LiveMarqueeAd.vue` | adapted | `entry.vue` marquee ad. |
| `components/LiveExternalLotteryTools.vue` | adapted | `entry.vue` comment-lottery and watch-reward floating entries. |
| `components/LivePlaybackDebugFloat.vue` | adapted | `entry.vue` debug panel gated by `debug=1`. |
| `composables/live-entry-initializer-helpers.js` | adapted | Route/auth/detail normalization handled by `live-route.js`, `h5-auth-context.js`, and `entry.vue`. |
| `composables/live-lottery-message.js` | adapted | Lottery text and socket notices handled by `entry.vue` marketing handlers. |
| `composables/useIMChannel.js` | adapted | H5 IM channel replaced by `MiniLiveSocket` event normalization. |
| `composables/useMessageChannel.js` | adapted | H5 message multiplexing replaced by `MiniLiveSocket` plus `handleSocketMessage`. |
| `composables/useLiveWebSocket.js` | adapted | `mini-live-socket.js` uses `uni.connectSocket`, reconnect, ping, replay request, sign key. |
| `composables/useLiveWsMessageHandler.js` | adapted | `entry.vue` socket dispatcher plus `mini-live-socket.js` normalized event names. |
| `composables/useLiveComments.js` | adapted | `entry.vue` history, optimistic send, pinned/top/delete/clear/filter, replay simulated comments. |
| `composables/useLiveChatInput.js` | adapted | `entry.vue` Mini Program input focus and send handling; H5 keyboard DOM listeners are not used. |
| `composables/useLiveEntryActions.js` | adapted | `entry.vue` like, product, share, center, tab, video tap actions. |
| `composables/useLiveBuyingNotice.js` | adapted | `entry.vue` transient buying notice and chat notice. |
| `composables/useLivePlaybackDebug.js` | adapted | `entry.vue` mini debug panel. |
| `composables/useLiveHeartbeatStatus.js` | adapted | `entry.vue` enter/heartbeat/leave plus socket status snapshots. |
| `composables/useLivePlayerInitializer.js` | adapted | `entry.vue` `live-player`/`video` setup and event handlers; no `hls.js`, `flv.js`, or DOM insertion. |
| `composables/useLiveReplayPlayback.js` | adapted | `entry.vue` replay list, progress restore/report, ended/auto-next. |
| `composables/useReplaySimOrders.js` | adapted | `entry.vue` windowed `getReplaySimMessages` consumption and order notice routing. |
| `composables/useLiveLoadBootstrap.js` | adapted | `entry.vue` `loadRoom`, auth guard, detail/stream/replay bootstrap. |
| `composables/useLivePlaybackWiring.js` | adapted | Mini playback events are wired directly in `entry.vue`. |
| `composables/useLiveScreenWakeLock.js` | adapted | `entry.vue` uses `uni.setKeepScreenOn`; H5 `navigator.wakeLock` is not used. |
| `composables/useLiveStageBinding.js` | adapted | Stage binding is static Vue template with native Mini Program components in `entry.vue`. |
| `composables/useLiveDisplayState.js` | adapted | `entry.vue` computed display state for overlays, poster, status, marquee, access/viewer-limit. |
| `composables/useLiveEntryHelpers.js` | adapted | Formatting/navigation helpers folded into `entry.vue` and `live-route.js`. |
| `composables/useLiveWatchRewards.js` | adapted | `entry.vue` watch reward list, entry, claim, socket refresh. |
| `composables/useLiveViewerMetrics.js` | adapted | `entry.vue` viewer/like display and status socket updates. |
| `composables/useLiveSidePanels.js` | adapted | `entry.vue` center/report/sign/product panels and navigation into migrated mini pages. |
| `composables/useLiveEntryBootstrap.js` | adapted | `entry.vue` load lifecycle and auth guard. |
| `composables/useLiveEntryLifecycle.js` | adapted | `entry.vue` `onShow`/`onHide`/`onUnload` keep-screen-on, status refresh, replay progress, heartbeat, leave, and socket teardown. |
| `composables/useLiveEnterNotice.js` | adapted | `entry.vue` enter/leave/system message display through chat stream. |
| `composables/useIOSWechatBridgeAutoPlay.js` | unsupported | H5 iOS WeChat bridge/autoplay is browser/JSSDK-only; mini implementation uses entry overlay user gesture with native `live-player`/`video`. |
| `composables/useLiveProducts.js` | adapted | `entry.vue` product list/current product/detail routing and product socket refresh. |
| `composables/useLiveProgressReport.js` | adapted | `entry.vue` replay progress storage/reporting. |
| `composables/useLiveVideoRuntime.js` | adapted | Browser runtime replaced by native `live-player`/`video` contexts in `entry.vue`. |
| `composables/useLiveCommentLottery.js` | adapted | `entry.vue` comment lottery entry, claim, socket refresh and panel. |
| `composables/useLiveAdaptiveQuality.js` | adapted | `live-route.js` stream candidate ranking and `entry.vue` status/failover; browser `navigator.connection` is not used. |
| `composables/useLivePurchase.js` | adapted | `entry.vue` buy reminder and navigation to migrated product/order mini pages. |
| `composables/useLiveMarketingRuntime.js` | adapted | `entry.vue` marketing state/actions/notices. |
| `composables/useLiveMuteState.js` | adapted | `entry.vue` mute toggle, user muted/unblocked/block socket events and mute-all settings. |
| `composables/useLiveEntryInitializer.js` | adapted | `entry.vue` detail bootstrap, access restrictions, stream lookup, replay first video, room context. |
| `composables/useLiveScheduleResume.js` | adapted | Replay/schedule resume is handled by replay progress restore and replay product schedule. |
| `composables/useLiveMiniWindow.js` | adapted | `entry.vue` landscape collapse/restore/hide mini-window behavior. |
| `composables/useLiveNormalLottery.js` | adapted | `entry.vue` normal lottery panel and participant lookup. |
| `composables/useLiveSoundIntent.js` | adapted | `entry.vue` entry overlay, manual play, mute toggle, sound-intent storage. |
| `composables/useLivePageLeave.js` | adapted | `entry.vue` `onUnload` teardown, progress report, leave API, socket close. |
| `composables/useLiveSubscribePush.js` | unsupported | H5 public-account/JSSDK subscribe flow has no safe Mini Program equivalent without backend template IDs; mini keeps live entry usable and share/auth native. |
| `utils/refresh-sound-intent.js` | adapted | Mini storage key `broadcast_sound_intent` and entry gesture in `entry.vue`. |
| `utils/entry-format.js` | adapted | Formatting logic folded into `entry.vue` and `live-route.js`. |
| `utils/entry-route.js` | adapted | `uniapp-src/src/utils/live-route.js`. |
| `utils/live-source.js` | adapted | `uniapp-src/src/utils/live-route.js` candidate ranking: RTMP, FLV, then HLS/video. |
| `utils/domain-prefetch.js` | unsupported | Browser `<link rel=prefetch>`/domain preconnect has no Mini Program DOM equivalent. |
| `utils/live-status-snapshot.js` | adapted | `entry.vue` `applyLiveStatusSnapshot` and candidate switching. |
| `utils/entry-init.js` | adapted | `live-route.js`, `h5-auth-context.js`, and `entry.vue` route/detail bootstrap. |
| `styles/entry-landscape.scss` | adapted | Scoped CSS in `entry.vue` landscape branch. |
| `styles/entry-global.scss` | adapted | Scoped page-level CSS in `entry.vue`; no global H5 DOM selectors. |
| `styles/entry-landscape-live.scss` | adapted | Scoped CSS in `entry.vue` live landscape branch. |
| `styles/entry-landscape-live-controls.scss` | adapted | Scoped CSS in `entry.vue` landscape controls. |
| `styles/entry-portrait.scss` | adapted | Scoped CSS in `entry.vue` portrait branch. |
| `styles/entry.scss` | adapted | Consolidated page CSS in `entry.vue`. |
| `styles/entry-overlays.scss` | adapted | Scoped overlay CSS in `entry.vue`. |
| `styles/live-portrait-stage.scss` | adapted | Scoped CSS in `entry.vue` portrait stage. |
| `styles/entry-live.scss` | adapted | Scoped CSS in `entry.vue` live status/product/chat controls. |
| `styles/live-landscape-stage.scss` | adapted | Scoped CSS in `entry.vue` landscape stage. |
| `styles/entry-shared.scss` | adapted | Shared page styles folded into `entry.vue`. |

## Unsupported Platform-Only Items

| Item | Reason |
|---|---|
| H5 iOS WeChat bridge autoplay | Requires browser DOM/JSSDK video access; mini uses user gesture plus native player contexts. |
| H5 public-account subscribe push | Requires H5/JSSDK public-account template flow; Mini Program needs backend template IDs for `requestSubscribeMessage`, which this H5 source does not provide. |
| H5 domain prefetch/preconnect | Requires browser DOM/link APIs; Mini Program has no equivalent preconnect primitive for page code. |

## Runtime Findings

- 2026-06-02 current hardcoded smoke room `wusrc6dh3wxd` is not live: `/h5/live/detail` and `/h5/live/streamInf` return `pushStatus: 2`, `liveStatusText: "未直播"`, and empty `pullUrl` / `pullFlvUrl` / `pullHlsUrl` / `pullRtmpUrl`. This room cannot prove live-player playback.
- The same room has replay data under `replays`, not `replayVideos`; sample item has `id: 574`, `videoUrl: https://replay.yaakoo123.cn/...m3u8`, and `duration: 760`. Mini Program entry initialization now normalizes `replayVideos`, `replays`, and single `replayVideo` before progress restore and video initialization.
- WeChat DevTools WXSS warnings can hide copied H5 styles. The broadcast product notice image now uses `.goodsPic` instead of `#goodsPic`, and product stock-line decoration uses real `.stock-line` views instead of Mini Program-ignored pseudo selectors.
- 2026-06-02 WXSS compatibility sweep fixed copied H5 selectors that WeChat WXSS rejects or silently ignores in live marketing/lottery surfaces. The affected components now use explicit classes or real nodes instead of copied H5 `#id`, child/tag, `:last-child`, `::before`, placeholder, and deep selectors. Covered files include `live-report-popup`, comment lottery password/slot/result modals, coupon receive/push/order modals, watch-duration reward modal, WeChat lottery modals, comment-prize confirm/rule modals, shake lottery modal, auth purchase modal, and red-luck detail modal.
- `npm run build:mp-weixin` passes after the WXSS selector sweep, and `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` for WeChat DevTools import.
- A follow-up build after removing the remaining `:deep(.wd-textarea...)` and `ShakeLotteryModals` child/nth selectors also passes. The refreshed dev package includes the expected class-based selectors such as `report-desc-textarea`, `shakeStartContent`, `shakeResultContent`, `shakePerfectField`, `commentLotteryPop-pic`, `goToComment`, and `source-goods-reward-record-link`.
- Static scan of broadcast/API/platform code after the follow-up build still has no executable browser-only APIs or H5 player/JSSDK imports. Remaining hits are limited to `info.windowHeight` in the Mini Program keyboard adapter and README text that documents avoiding browser upload APIs.
- The old pasted DevTools warning `defineAsyncComponent is unsupported` does not match current source: `LiveBroadcastMarketingLayer.vue` now uses static component imports and no source scan hit remains for `defineAsyncComponent`.
- The earlier `videoPlayer is not defined` and `handlePageHide is not defined` runtime errors are fixed in source: `entry.vue` declares/passes the mini media context getter/setter and destructures `handlePageHide`/`handlePageBackground` from `useLivePageLeave`.
- 2026-06-02 follow-up WXSS hardening replaced the remaining copied H5 pseudo/structural selectors in broadcast and lottery/reward surfaces with Mini Program-safe real nodes/classes while keeping the H5 visual class structure. Covered examples: live anchor fire/count icons, manual play triangle, chat quick-reply arrow, debug float buttons, watch-reward coupon info block, reward-panel avatar selector, and preview buttons.
- `npm run build:mp-weixin` passes after the follow-up hardening. `npm run dev:mp-weixin` was also run long enough to refresh `uniapp-src/dist/dev/mp-weixin`, then the watch process was stopped.
- Static scans of `uniapp-src/src/pages/broadcast`, `uniapp-src/src/components/live-lottery`, `uniapp-src/src/pages/lottery-preview`, and the corresponding `dist/dev/mp-weixin` output show no remaining high-risk selectors (`::before`, `::after`, `:not`, `:nth-child`, id selectors excluding color literals) and no real browser-only runtime APIs or H5 player/JSSDK imports in the live-room scope.
- The generated dev WXML contains both native playback branches: `live-player` for live-source candidates and `video` for replay/video candidates in portrait and landscape broadcast stages.
- 2026-06-02 playback-context hardening fixed a Mini Program runtime risk introduced by the componentized 1:1 migration: `id="liveVideo"` lives inside `LivePortraitStage` / `LiveLandscapeStage`, while playback control was initiated from the parent `entry.vue`. `LivePortraitStage`, `LiveLandscapeStage`, and `LiveBroadcastStageHost` now expose `createMediaContext`, `createVideoContext`, and `createLivePlayerContext`, and parent playback paths (`useLivePlayerInitializer`, `useLiveVideoRuntime`, `useLiveWsMessageHandler`, `videoPlay`) prefer that scoped context before falling back to page-level `uni.create*Context`.
- The same bridge is used by `useLiveChatInput` through `stageHostRef`, so parent-triggered `focusInput()` / `blurInput()` reaches the actual H5-copied `LiveChatBar` input inside the active stage component instead of disconnected parent refs.
- `npm run build:mp-weixin` passes after the scoped media/input context bridge. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin`; the generated dev output contains `stageHostRef` and scoped `createMediaContext` calls in entry, host, portrait stage, landscape stage, playback initializer, runtime resume, and WS video-loop restart paths.
- 2026-06-02 share/product interaction hardening fixed two concrete live-room dead links:
  - `share-popup` "生成邀请函" now targets registered `/pages/invitation/index`; the page exists in `uniapp-src/src/pages/invitation/index.vue`, uses H5 invitation template assets/slot coordinates, reads `invitation_payload`, generates a local QR code, supports link copy, QR save, and Mini Program share hooks without H5 `document`/`Image` canvas or JSSDK.
  - `share-popup` no longer imports/calls H5 WeChat JSSDK helpers. It exposes Mini Program share guidance, link copy, QR generation, and uses `src/platform/weixin/file.js` to write QR data URLs to `USER_DATA_PATH` before `saveImageToPhotosAlbum`.
  - Live product detail routing now navigates to the registered Mini Program route `/pages/product/detail/detail?product_id=...` instead of the unregistered `/pages/product/detail?...`, preserving `roomId`, `termId`, and SKU when present.
  - `entry.vue` now registers `onShareAppMessage`/`onShareTimeline` so native Mini Program share cards carry the current broadcast route, room code/live id/tenant id, cover, and title.
- `npm run build:mp-weixin` passes after the share/product fixes. `dist/dev/mp-weixin` was refreshed; scans confirm `pages/invitation/index` is registered in generated `app.json`, no live-room source hit remains for `window`, `document`, `localStorage`, `sessionStorage`, `navigator`, `XMLHttpRequest`, `Blob`, `hls.js`, `flv.js`, `weixin-js-sdk`, `@dcloudio/uni-h5`, `defineAsyncComponent`, `createElement`, `appendChild`, or `fetch(`.
- Route coverage script confirmed `pages/broadcast/entry`, `pages/broadcast/replay`, `pages/invitation/index`, `pages/invitation-record/index`, `pages/prize-record/index`, `pages/report/index`, and `pages/product/detail/detail` are registered and have matching `.vue` files.
- Invitation page limitation: the current Mini Program implementation renders the H5 invitation visual as layered native views/images and saves the QR code. It does not yet export a single fully composited poster image because the H5 source used browser-only offscreen canvas APIs; full poster export would need a Mini Program canvas implementation.
- 2026-06-02 component WXSS reset fix: global `page/view/text/button/input/textarea/image` reset rules were moved out of `uni.scss` into `app-global.scss` and imported only from `App.vue`. `uni.scss` is injected by uni-app into every component style block, so keeping global tag selectors there produced Mini Program component WXSS warnings and caused copied H5 live-room styles to be ignored. Product shelf sold-out labels were also changed from `text` tag selectors to `.stock-state-text` class selectors while preserving the H5 class structure. `npm run build:mp-weixin` passes, `dist/dev/mp-weixin` was refreshed, and a selector parser scan of 23 live-room/product/marketing `.wxss` files in both build and dev output found no component-level tag, id, attribute, or unsupported structural/element pseudo selectors.
- 2026-06-02 H5 quick-reply popover positioning was restored in the Mini Program `LiveChatBar`: the H5 placement math is still used, but DOM `querySelector/getBoundingClientRect` calls were replaced by `uni.createSelectorQuery().in(component)`. The generated `dist/dev/mp-weixin/pages/broadcast/components/LiveChatBar.*` contains the active eye class and selector-query placement path, so the copied H5 quick-reply bubble follows the clicked icon instead of falling back to a fixed centered position.
- 2026-06-02 live playback source selection now merges `/h5/live/detail` and early `/h5/live/streamInf` candidates before initializing or re-initializing native playback. This keeps RTMP `live-player` first on device, FLV `live-player` second, and HLS `video` as fallback even when the detail payload lacks one of the playable Mini Program URLs. A focused Node sanity check confirmed `detail.pullFlvUrl + streamInf.pullRtmpUrl/pullHlsUrl` sorts as RTMP, FLV, HLS, and `npm run build:mp-weixin` passes after the change. `dist/dev/mp-weixin` was refreshed and contains `earlyLiveStreamInfo` / `mergePullStreams`.
- A follow-up generated-WXSS parser scan of 26 live-room/product/lottery/reward/sign `.wxss` files in `dist/dev/mp-weixin` found no unsupported component tag selectors, element pseudo selectors, universal selectors, or forbidden declarations.
- Remaining live-playback blocker is test data, not a proven code pass: a currently live room with `pushStatus = 1` and non-empty Mini Program-playable pull URL is still required before claiming live-player playback, real-time comments, live lottery, and watch reward behavior are fully usable on device.
- 2026-06-02 playback exhaustion handling was added to the Mini Program native playback adapter. `useLivePlayerInitializer` now allows replay fallback candidate switching instead of skipping all `isReplay` failures, records exhausted playback with `mini_player_failure_exhausted`, clears the state on `markPlaybackReady`, and exposes `retryPlayback()` to restart from the first known candidate. `entry.vue`, `useLiveStageBinding`, `LivePortraitStage`, and `LiveLandscapeStage` pass this state into the copied H5 stage templates and render a centered `playback-failure` retry overlay without changing the surrounding H5 layout/classes.
- `npm run build:mp-weixin` passes after the playback exhaustion fix. Static source scan of `uniapp-src/src/pages/broadcast`, `uniapp-src/src/platform`, and `uniapp-src/src/api` still finds no forbidden browser globals or H5 player/JSSDK imports. Route checks pass for `pages/broadcast/entry`, `pages/broadcast/replay`, `pages/live/live-vertical`, and `pages/live/live-horizontal` in both `src/pages.json` and generated `dist/build/mp-weixin/app.json`.
- `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 21:34:51. The generated dev package contains `playback-failure`, `retryPlayback`, and `mini_player_failure_exhausted`; dev route checks pass for the same four live routes, and a parser scan of 24 generated live/lottery `.wxss` files found no unsupported component tag selectors, structural/element pseudo selectors, or universal selectors.
- 2026-06-02 Mini Program WebSocket event normalization was hardened for H5 backend event envelopes. `mini-live-socket.js` now preserves top-level and nested `event` / `eventType` / `msgType` / `name` / `cmd` / `action`, normalizes camelCase/kebab-case/snake_case names through one matcher, supports watch reward lifecycle/broadcast aliases, live status snapshot aliases, product status aliases, and comment lottery H5 names such as `beginCommentLotteryPrize`, `openPrize`, and `updateCommentLotteryConfig`. Numeric message type `29` is now normalized to the H5-aligned `comment_lottery_event` instead of the broader `comment_lottery`.
- A focused Node sample check verified key Mini Program socket inputs map to the handler-visible event types: `watchRewardLifecycle`, `watch-reward-broadcast`, `liveStatusUpdate`, `beginCommentLotteryPrize`, numeric `29`, nested `data.event=updateCommentLotteryConfig`, `productStatusUpdate`, and `viewerCount`. `npm run build:mp-weixin` passes after the change; static browser-only source scan still passes; route checks for the four live routes still pass; build WXSS parser scan covered 24 generated live/lottery `.wxss` files. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` at 2026-06-02 21:44:10 and the generated dev socket adapter contains the new normalization logic.
- 2026-06-02 live comment and WebSocket business handling were hardened for mixed H5 socket payload shapes. `useLiveWsMessageHandler.js` now reads chat, replay comment, comment-top/delete, product status/list/stock, mute/block, watch reward, and comment-lottery fields from nested `data`, nested `payload`, or top-level flattened H5 envelopes. This fixes cases where Mini Program normalization flattened a message but the copied H5 handler still read only `data.data.commentId`, `data.data.isTop`, `data.data.replayVideoId`, `data.data.action`, or `data.data.commentIds`.
- `useLiveComments.js` now allows `/h5/live/comment` HTTP fallback when the Mini Program socket object is not created or is reconnecting, instead of failing before the fallback branch. When the fallback succeeds, the optimistic local chat item is upgraded from the API response if `commentId` / `msgId` / `seq` are returned. Comment history normalization also accepts both H5 nested `item.data.*` and flattened fields for replay timelines and live history.
- Focused source-loader tests verified handler behavior for nested chat metadata, flattened `comment_top`, flattened `comment_delete`, flattened replay chat timeline insertion, camelCase comment lottery events, nested comment lottery open events, and watch reward lifecycle callback routing. `npm run build:mp-weixin` passes after the change; static browser-only scan of broadcast/API/platform/socket source still passes; `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 21:56:02 and the generated dev output contains `getDataPayload`, `getNestedPayload`, and the HTTP fallback path.
- 2026-06-02 ended-live replay fallback now routes the hardcoded smoke room shape through the copied replay playback path instead of initializing only a raw video URL. When `/h5/live/detail?roomCode=wusrc6dh3wxd` returns `pushStatus: 2`, `hasReplay: true`, and `replays[0]`, `useLiveEntryInitializer` sets `isReplay`, marks the display state as `回放`, stores the replay video id/index, and calls `playReplayVideoByIndex(0, 0)`. This preserves replay comments, simulated orders, product schedule, and progress behavior for ended live rooms with replay data.
- `LivePortraitStage` and `LiveLandscapeStage` now prefer `isReplay` for the status pill text/class, so rooms reported as `未直播` by the live detail API but rendered from replay data show the H5 replay state instead of an ended-live label.
- Verification after the ended-live replay fallback: `npm run build:mp-weixin` passes; route coverage passes for main-package and subpackage audience routes including `pages/broadcast/entry`, `pages/broadcast/replay`, `pages/live/live-vertical`, `pages/live/live-horizontal`, `pages/product/detail/detail`, `pages/order/list`, `pages/order/detail`, `pages/order/confirm`, `pages/address/index`, `pages/address/edit`, and `pages/center/index`. Static scan of the live-room scope still has only known false positives: CSS class `slot-machine-window`, README text documenting forbidden browser upload APIs, and WeChat Mini Program `windowHeight` system-info field.
- Current test-data limitation remains: `wusrc6dh3wxd` is not a live room and `/h5/live/streamInf` returns no live pull URL, so this room can validate replay fallback only. A room with `pushStatus: 1` plus a Mini Program-playable RTMP/FLV/HLS URL is still required before declaring live-player playback, real-time barrage, live lottery, and watch reward behavior fully verified on device.
- 2026-06-02 live-entry replay behavior was tightened again: playback-time replay features now key off `isReplay && replayCurrentVideoId` instead of only `roomGroupType === 1`. This prevents ended live rooms opened through `/pages/broadcast/entry` from degrading to plain live-history comments after the video fallback starts. The affected paths are replay comment timeline loading (`limit=200`, `replayVideoId`), replay progress flush on hide/unload, replay switch progress reporting, and periodic replay progress reporting. Column-only cross-term/end-of-term behavior still remains guarded by `roomGroupType === 1`.
- Verification after this tightening: `npm run build:mp-weixin` passes; static live-room scan still only reports the known false positives (`slot-machine-window`, README documentation text, Mini Program `windowHeight` field); route coverage still passes for the audience live/order/address/center/report/prize/invitation routes; `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 22:25:53, and generated dev output contains `shouldUseReplayTimeline`.
- 2026-06-02 replay term-context hardening: `getEffectiveTermId()` now uses the current replay video/list whenever `isReplay` is true, not only for replay-column rooms. This keeps live-entry replay product-detail, purchase context, and comment-lottery term context from falling back to `termId=0`. The initial post-enter replay progress report also now keys off `isReplay && roomCurrentTermId && replayCurrentVideoId`.
- Verification after term-context hardening: `npm run build:mp-weixin` passes; the browser-only static scan and route coverage still pass with the same known false positives; `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 22:29:14, and generated dev output contains the `isReplay` term-context checks.
- 2026-06-02 ended-live replay initialization order was corrected for Mini Program smoke data. The real `/h5/live/detail?roomCode=wusrc6dh3wxd` response is `pushStatus: 2`, `liveStatusText: "未直播"`, `hasReplay: true`, with replay data under `replays[0].videoUrl`. `initDetailVideoFlow` now lets this shape fall through into `initLiveDetailPlayback` replay fallback instead of stopping at the "未开播/封面" branch, so the native `<video>` replay path can initialize from `playReplayVideoByIndex(0, 0)`.
- 2026-06-02 marketing activity guard was also made replay-aware. `useLiveMarketingRuntime` now blocks marketing overlays/entries on the entry overlay or non-replay ended-live state only; replay playback is no longer treated as a live-ended state that force-closes watch rewards, comment lottery, and related marketing surfaces.
- Verification after the replay/marketing guard fix: `npm run build:mp-weixin` passes; static scan across `src/pages/broadcast`, `src/components/live-lottery`, and `src/pages/lottery-preview` found no actionable browser-only APIs or unsupported H5 player/JSSDK imports; route coverage passes for `pages/broadcast/entry`, `pages/broadcast/replay`, `pages/live/live-vertical`, and `pages/live/live-horizontal` including subpackages. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 22:40:56, and generated dev output contains `canFallbackToReplay` plus the replay-aware `marketingBlocked` expression. Duplicate historical `dev:mp-weixin` watch processes for this workspace were stopped so they cannot overwrite the refreshed dev package.
- 2026-06-02 live entry hardcoded test override was removed from the default path. `useLiveEntryBootstrap` still preserves the supplied `wx_token` into Mini Program storage, but it no longer overwrites arbitrary real room links with the smoke room `wusrc6dh3wxd`; the test room is available only through an explicit `useTestLive=1`/`testLive=1`/`debugLive=1`/`debug_live=1` parameter. This is required before any real live room can be verified.
- The same pass routes all entry options through `normalizeLiveRouteOptions`, so legacy `scene=live_id:123` and `live_id=123` are normalized into `liveId=123` instead of being ignored. The H5 `/h5/live/detail` API was checked directly and accepts `roomCode` only; `roomId=232`, `liveId=232`, `live_id=232`, and `id=232` all return `RoomCode required`. Therefore links without a `roomCode` now show a clear Mini Program toast (`缺少直播间口令，请使用新版直播链接`) instead of silently entering the wrong test room.
- `pages/broadcast/replay` now redirects through `buildBroadcastEntryUrl`, preserving `roomCode`, `liveId`, `tenantId`, `liveType`, `_tc`, `wx_token`, cover/title, and `replay=1`. `buildBroadcastEntryUrl` was expanded to retain those H5 route parameters for both replay and live entry compatibility.
- Verification after entry-route hardening: a focused Node route check confirmed `scene=live_id:123` becomes `/pages/broadcast/entry?liveId=123`, and `roomCode=abc&tenantId=15&liveType=live&_tc=x&wx_token=tok` is preserved. `npm run build:mp-weixin` passes; static scan across 119 broadcast/lottery/replay route files has zero actionable browser-only or H5-only imports; route coverage passes for broadcast entry/replay, old live vertical/horizontal subpackage routes, prize record, invitation record, and report routes. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 22:48:21, then the watch process was stopped.

- 2026-06-02 Mini Program enter-live gesture playback hardening: `useLiveSoundIntent` no longer returns early in WeChat DevTools after only updating mute state, and `useLivePlayerInitializer.runPlayerCommand("play")` no longer pretends DevTools playback succeeded without calling the scoped native `video` / `live-player` context. This keeps the copied H5 entry overlay behavior while ensuring a user tap actually reaches the Mini Program media component, which is required for live/replay playback smoke testing.
- Verification after the gesture playback hardening: `npm run build:mp-weixin` passes; H5 broadcast visual file/class coverage remains complete (`26/26` visual files, zero missing H5 classes); static scan of broadcast/live-lottery/lottery-preview/utils/services/api has zero actionable browser globals or H5-only player/JSSDK imports; route coverage passes for broadcast entry/replay, old live vertical/horizontal routes, product detail, order list/detail/confirm, address, center, report, prize record, and invitation record. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 22:58:42 and the generated playback files contain the native play path.
- 2026-06-02 viewer-loop dead-end cleanup: live-room personal-center unknown module actions no longer terminate in `功能开发中`; they fall back to the real `/pages/center/index` route while preserving `roomCode`. Order detail `pay` now routes to `/pages/order/pay`, refund-progress actions route to `/pages/order/refund-detail` or the refund list fallback, and unknown order detail actions route back to the real order list instead of a placeholder toast.
- 2026-06-02 lottery result H5 route compatibility: shared `route-navigation.js` now extracts Mini Program routes from H5 full URLs such as `https://...#/pages/...`, encoded H5 routes, and plain `/pages/...` values before calling `uni.navigateTo`. WeChat lottery result modals in both live-room and lottery-preview scopes use this adapter, so H5 prize/order links that point at migrated pages no longer fail as generic external links. True external non-page URLs remain an explicit Mini Program boundary and guide the user to prize records.
- Verification after the dead-end cleanup: `npm run build:mp-weixin` passes. Route coverage script reports 195 registered routes, no duplicate routes, no missing `.vue` files, and no missing audience-loop required routes (`broadcast`, legacy live redirect, center, order/pay/refund/receipt, address, report, prize record, invitation record). Static scan of the audience-loop scope has no `TODO:migration`, `开发中`, `暂不支持`, `not implemented`, empty-shell text, H5 player imports, JSSDK imports, or actionable browser globals; remaining scan hits are explicit Mini Program boundaries such as `agoraFlsPlayer` rejecting H5 Agora DOM playback and `bindid` rejecting H5 cross-domain binding.
- Focused route/playback sanity checks after the same pass confirmed `scene=live_id:123`, `live_id=123`, `roomCode=xxx`, and `replay=1` normalize as expected, and `getMiniProgramLiveCandidates` still sorts RTMP `live-player` before FLV `live-player` before HLS `video`. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 23:10:08; generated dev output contains `extractMiniProgramRoute` and the updated center fallback. No `dev:mp-weixin` watch process was left running.
- Current live-playback verification boundary remains unchanged: the supplied smoke room `wusrc6dh3wxd` currently returns `pushStatus: 2`, `liveStatusText: "未直播"`, no live pull URL from `/h5/live/detail`, no pull URL from `/h5/live/streamInf`, and no first replay video from `/h5/live/replayFirstVideo`. It cannot prove live-player playback, live barrage, live lottery, or watch reward behavior on device. A currently live room with `pushStatus: 1` plus a Mini Program-playable RTMP/FLV/HLS URL is still required before declaring the full no-payment viewer loop verified.
- 2026-06-02 share fallback route hardening: `share-popup` no longer falls back to `/pages/broadcast/entry?roomId=...` when distributor share URL and explicit `linkUrl` are absent. The broadcast stage binding now passes `roomCode` and `liveTenantId` into the copied H5 portrait/landscape stages, both stages pass them through to `share-popup`, and the generated fallback Mini Program route is `/pages/broadcast/entry?roomCode=...&liveId=...&tenantId=...`. This matters because the H5 `/h5/live/detail` backend requires `roomCode`; a `roomId`-only copied/share QR link cannot enter the room.
- Verification after the share fallback fix: `npm run build:mp-weixin` passes with only Sass deprecation warnings. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 23:20:20 and the watch process was stopped. Source/build/dev scans find no remaining `broadcast/entry?roomId` fallback. Generated build/dev output contains `miniProgramRoomLink`, stage-level `room-code` binding, and `roomCode`/`tenantId` props on `share-popup`. Route coverage still reports 195 registered routes and passes for broadcast entry/replay, legacy live vertical/horizontal, prize record, invitation record, report, order list/detail, and address. A precise forbidden-runtime scan of broadcast/lottery/share/api/services/utils/platform code reports only CSS class name false positives (`slot-machine-window`) and no real `window`, `document`, `localStorage`, `sessionStorage`, `navigator`, H5 player, or JSSDK imports.
- 2026-06-02 Mini Program subscribe reminder config hardening: `useLiveSubscribePush` now uses `liveTenantId` and `domainStore.load(tenantId)` before calling `uni.requestSubscribeMessage`. This preserves the H5 tenant-based subscribe-template lookup while keeping the Mini Program native subscription flow, and prevents a false `未配置订阅模板` state when the domain store has not been loaded for the current live tenant.
- Verification after subscribe reminder hardening: `npm run build:mp-weixin` passes with only Sass deprecation warnings. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 23:29:41 and the watch process was stopped. Generated dev/build output contains `ensureSubscribeConfigLoaded`, `domainStore.load(tenantId)`, and `requestSubscribeMessage`, with no H5 `mp.weixin.qq.com/subscribemsg` redirect, JSSDK dependency, or browser global subscription path. Static scan still reports only the known CSS class-name false positive `slot-machine-window`; route coverage remains at 195 registered routes with the required audience-loop routes present.
- 2026-06-02 Mini Program WebSocket signature hardening: `useLiveWebSocket` now clears any stale global signing key before each new connection, extracts `/h5/live/wsSignKey` from string, camelCase, snake_case, or nested `data` response shapes, stores it with `setSignKey`, and passes it into `MiniLiveSocket` as `signKey`. `MiniLiveSocket.send()` now calls `wrapMessage(body, this.signKey || undefined)`, so an explicit empty instance key falls back to the current global key instead of forcing unsigned outgoing messages. This directly protects live-room enter, heartbeat ping/pong, comments, likes, and activity-related socket sends when the H5 backend requires signed envelopes.
- Verification after WebSocket signature hardening: focused Node checks confirm `wrapMessage` produces a signed envelope with a key and plain JSON without one. `npm run build:mp-weixin` passes with only Sass deprecation warnings. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 23:39:06 and no watch process remains. Generated dev/build output contains `resolveWsSignKey`, nested `secret_key` compatibility, `signKey: wsSignKey`, and `wrapMessage(body, this.signKey || void 0)`. Static scan still reports only the CSS class false positive `slot-machine-window`; route coverage remains 195 registered routes with the required audience-loop routes present.
- 2026-06-02 H5 auth session runtime-store sync: `syncH5AuthSession` now updates both Mini Program storage and the Pinia `useUserStore()` runtime state, and `clearH5AuthSession` clears the store as well. This fixes the login-page / silent-login return path where `h5_token` could exist in storage while `userStore.token` stayed stale in the same Mini Program process. The affected live-room paths include WebSocket connection token, enter/leave/heartbeat guards, sign-in, lottery/reward auth checks, and comment/like fallback decisions.
- Verification after auth runtime-store sync: source checks confirm `syncRuntimeUserStore`, `userStore.setToken(token)`, `userStore.setUserInfo(customer)`, and `useUserStore().clearAuth()` are present. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Static scan still reports only the CSS class false positive `slot-machine-window`; route coverage remains 195 registered routes with required audience-loop routes present. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-02 23:44:37 and generated dev output contains the Pinia token/user sync plus auth clear path; no watch process remains.
- 2026-06-02 H5 request error-shape hardening for migrated activity loops: `h5Request` now wraps HTTP, business, auth, and network failures in `Error` objects with `message`, `response`, `data`, `code`, `statusCode`, and `raw`. This preserves the H5 request semantics expected by copied watch-reward, comment-lottery, sign-in, report, and purchase handlers that read `err.message`, `err.response.msg`, or `err.response.data`. It prevents states such as watch-duration-not-reached, sold-out, activity-closed, or auth-expired from degrading into generic or structurally missing errors in Mini Program runtime.
- Verification after request error-shape hardening: source checks confirm `normalizeH5Error`, `error.response = body`, `error.data = body.data`, business-error wrapping, and the Mini Program network fallback message are present. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Static scan of the audience live scope still reports only the known CSS class-name false positive `slot-machine-window`; route coverage remains 195 registered routes with required audience-loop routes present. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin/api/h5.js` at 2026-06-02 23:50:22, generated dev output contains `normalizeH5Error`, `登录已失效`, and `网络异常，请稍后重试`, and no `dev:mp-weixin` watch process remains.
- 2026-06-02 Mini Program comment HTTP fallback payload compatibility: the migrated Mini Program comment sender keeps the copied H5 WebSocket send path unchanged, but its Mini-only `/h5/live/comment` fallback now sends `content` / `comment` plus the original nested `data` and flat `msgId`, `clientMsgId`, `timelineSeconds`, `replayVideoId`, `videoId`, and `termId` fields when present. This protects barrage/comment sending and comment-lottery participation during socket reconnect or socket creation failure, including replay timeline comments, against backend parsers that only read flat H5 comment metadata.
- Verification after comment fallback compatibility: source checks confirm `buildLiveCommentPayload` and the flat replay/client-message metadata fields are present. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Static scan of broadcast, live-lottery, lottery-preview, API, services, platform, and utils scope found no actionable browser globals or H5 player/JSSDK imports after filtering known false positives. Route coverage remains 195 registered routes with required audience-loop routes present. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin/api/live.js` at 2026-06-02 23:57:42, and generated dev output contains the flat comment fallback fields plus `/h5/live/comment`; no `dev:mp-weixin` watch process remains.
- 2026-06-03 Mini Program presigned-upload fallback hardening: `platform/weixin/file.js` now strictly checks the `uni.uploadFile` fallback response status when raw file-read + `uni.request(PUT)` is unavailable. Complaint and refund proof uploads both use this shared adapter, so failed multipart fallback uploads now surface as `OSS上传失败: HTTP ...` instead of being treated as successful evidence uploads. This keeps the migrated complaint/refund non-payment loop honest on Mini Program bases where raw file reads are restricted or presigned PUT endpoints reject multipart uploads.
- Verification after upload fallback hardening: source checks confirm the fallback stores `const response = await uploadFile`, accepts only `2xx`, and throws `OSS上传失败` otherwise. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Static scan of broadcast, live-lottery, lottery-preview, API, services, platform, and utils scope found no actionable browser globals or H5 player/JSSDK imports after filtering known false positives. Route coverage remains 195 registered routes and includes broadcast entry/replay, report form, refund, prize record, and address routes. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin/platform/weixin/file.js` at 2026-06-03 00:02:39, and generated dev/build output contains the strict upload fallback status checks; no `dev:mp-weixin` watch process remains.
- 2026-06-03 H5 request success-code strictness: the Mini Program `/h5/*` request layer now uses `isSuccessfulH5Response` instead of `Number(body.code) === 0 || Number(body.code) === 200`. This preserves the H5 source contract where only `code: 0` is success, while still accepting string `"0"` / `"200"` and explicit `success: true`. It no longer treats `code: null`, empty strings, or whitespace as success, preventing malformed or controller-adjacent responses from flowing into live detail, comments, sign-in, lottery/reward, complaint, address, or order handlers as if they were valid data.
- Verification after success-code strictness: focused Node checks confirm `0`, `"0"`, `200`, `"200"`, and `success: true` pass, while `null`, `""`, whitespace, and `1` fail. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Static scan of broadcast, live-lottery, lottery-preview, API, services, platform, and utils scope found no actionable browser globals or H5 player/JSSDK imports after filtering known false positives. Route coverage remains 195 registered routes with required audience routes present. Build output uses trimmed string checks rather than `Number(code)`, and `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin/api/h5.js` at 2026-06-03 00:08:00 with the strict success-code logic; no `dev:mp-weixin` watch process remains.
- 2026-06-03 live product-detail context preservation: `useLiveEntryActions.onProductDetail` now carries `roomCode` and `tenantId` into `/pages/product/detail/detail` alongside `product_id`, `roomId`, `termId`, and SKU. This closes a live-room context loss where the product detail / downstream order path could retain only a numeric live id while the H5 backend and return-to-live flow require the room code.
- 2026-06-03 H5 address-list route compatibility: registered `/pages/address/list` and added a Mini Program redirect page that preserves all query params into the already migrated `/pages/address/index` implementation. This prevents H5-oriented personal-center or live-room address links from landing on an unregistered page while still using the real H5-address API/list/edit/delete/import flow.
- Verification after product/address context hardening: `npm run build:mp-weixin` passes with only Sass deprecation warnings. Route coverage passes for 16 core audience routes including `pages/broadcast/entry`, `pages/broadcast/replay`, legacy live vertical/horizontal, center, order/pay/receipt, `pages/address/list`, `pages/address/index`, `pages/address/edit`, report, prize record, and invitation record, with matching `.vue` files. Static scan across broadcast, API, platform, and shared components reports only known false positives where `window` appears in CSS class names or Mini Program CSS variables (`slot-machine-window`, `diy-window`, `--window-top`), and no actionable browser global, H5 player, JSSDK, or Agora DOM dependency. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 00:17:25; generated output includes `pages/address/list` in `app.json` and product-detail route context containing `roomCode` / `tenantId`. No `dev:mp-weixin` watch process remains.
- 2026-06-03 share attribution guard hardening: `share-popup` now declares and consumes `isDistributor` / `distributorStatus`, and only calls `/h5/live/getDistributorShareUrl` when `isDistributor && distributorStatus === 1`. The copied portrait and landscape stages pass these values through to the popup. Ordinary viewers therefore use the Mini Program broadcast route fallback directly instead of hitting the distributor-only share endpoint or waiting on an endpoint that should not apply to them.
- Verification after share attribution guard hardening: focused source checks confirm the distributor guard appears before `getLiveDistributorShareUrl(rid)` and both `LivePortraitStage` / `LiveLandscapeStage` pass `is-distributor` plus `distributor-status` into `share-popup`. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Forbidden runtime scan of broadcast/share/API/platform/services scope has zero hits for browser globals, H5 player packages, JSSDK, or Agora DOM APIs. Route coverage for 16 core audience routes still passes. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 00:24:52, and generated output contains `canUseDistributorShare` plus stage-level distributor bindings; no `uni`/`vite` process remains.
- 2026-06-03 comment-lottery send-path hardening: all live-room comment send entry points now go through the same marketing wrapper. `LivePortraitStage` and `LiveLandscapeStage` route input `confirm`, send-button `send`, and quick-reply taps into `handleSendClick`; `useLiveComments.handleSendClick` forwards optional quick-reply text into `sendMessage`; `useLiveMarketingRuntime.handleCommentLotterySendClick` accepts optional text and only calls `handleCommentLotteryCommentSent(text)` when comment sending does not fail. This closes a hidden path where barrage/comments could send successfully but quick replies or keyboard-confirmed comments would not trigger comment-lottery participation handling.
- Verification after comment-lottery send-path hardening: focused source checks confirm portrait/landscape `@confirm` and `@send` both bind `handleSendClick`, quick replies call `handleSendClick(text)`, the marketing wrapper accepts `overrideText`, and the lottery callback is guarded by `sent !== false`. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Forbidden runtime scan of broadcast/share/API/platform/services scope has zero hits. Route coverage for 16 core audience routes still passes. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 00:30:04, and generated output contains `handleCommentLotterySendClick` plus `handleSendClick(text)` in both stage components; no `uni`/`vite` process remains.
- 2026-06-03 sign-check state hardening: local H5 proxy verification for room `wusrc6dh3wxd` returned `/h5/live/sign/check` as `{"enabled":false,"signed":false}`. `useLiveSidePanels.loadSignStatus` now normalizes `signed` / `hasSigned` / `isSigned` / `signStatus` / `status` and `enabled` / `signEnabled` / `enableSign` / `isEnabled`. When the check API explicitly says sign-in is disabled, Mini Program clears `signConfig.enabled`, clears `forceEnabled`, clears fields, and closes the sign popup. This prevents stale detail-side `signConfig.enabled` from forcing users into a disabled sign-in flow.
- Verification after sign-check state hardening: focused Node checks cover `enabled:false`, string/numeric enabled flags, `hasSigned`, `isSigned`, and `signStatus:"done"`. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Forbidden runtime scan of broadcast/live-sign/API/platform/services scope has zero hits. Route coverage for 16 core audience routes still passes. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 00:35:29, and generated output contains `normalizeSignCheckResult`, `signStatus.enabled === false`, and the force-enabled popup close path; no `uni`/`vite` process remains.
- 2026-06-03 comment-lottery one-click password propagation hardening: `comment-prize-rule-modal` and `CommentLotteryPasswordModal` now emit the visible participation password/condition text with `send-comment`. `LiveBroadcastMarketingLayer` forwards that payload to `marketingRuntime.sendCommentLotteryText`, and `useLiveCommentLottery.sendCommentLotteryText(comment)` prioritizes the explicit modal text before falling back to H5 internal active-prize/tip state. This preserves the H5 copied UI/event shape while making the Mini Program one-click comment-lottery entry stable when activity state refreshes race with the popup.
- Verification after one-click password propagation: `npm run build:mp-weixin` passes with only Sass deprecation warnings. Forbidden runtime scan of broadcast, comment-lottery components, API, platform, and services has zero hits for browser globals, H5 player packages, JSSDK, or Agora DOM APIs. Route coverage for 16 core audience routes passes when main package and `subPackages` are both expanded. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin`; generated output contains `emit("send-comment", conditionText.value)`, `emit("send-comment", displayPasswordText.value)`, forwarded `send-comment-lottery` payloads, and `sendCommentLotteryText(comment)`. No `uni`/`vite` watch process remains.
- 2026-06-03 normal-lottery H5 envelope compatibility hardening: `useLiveNormalLottery` now unwraps Mini Program socket messages with `unwrapLotteryPayload`, merges nested `data.payload`, nested `data`, and top-level fields, and reads winner/participant arrays from H5-compatible aliases (`winners`, `winnerList`, `records`, `newRecords`, `results`, `participants`, `participantList`, `users`, `userList`). Lottery result, personal win notify, and win-record update now share this normalized payload path. Debug-only normal-lottery and stage-host modal logs were removed to keep real-device console output readable during playback/lottery testing.
- Verification after normal-lottery compatibility hardening: focused Node sanity check confirms nested `data.payload.newRecords/participantList` opens `wechatLotteryCountdown` and records winner/participant data, while a top-level `payload.results` win-record update appends a new winner. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Forbidden runtime scan reports only the known CSS class-name false positive `slot-machine-window`, with no actionable browser global, H5 player package, JSSDK, or Agora DOM usage. Route coverage for the 16 core audience routes passes with main package plus `subPackages` expanded. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 00:49:00; generated output contains `normalizeLotteryPayload` / `pickLotteryList` and no `normalLotteryActiveModal computed` or `[NormalLottery]` debug logs. No `uni`/`vite` watch process remains.
- 2026-06-03 live product payload compatibility hardening: product mapping now treats `productId`, `product_id`, `goodsId`, and `goods_id` as canonical ids, and maps H5-style `productImage/product_image/goodsPic/goods_pic` plus `productName/product_name/goodsName/goods_name` into the copied product card/list UI. The live WebSocket product handler now normalizes product ids, product actions (`explain`, `explaining`, `current_product`, `soldout`, etc.), multi-explaining id arrays, and product list aliases (`products`, `productList`, `list`, `goodsList`, `goods`). Product讲解卡片、货架刷新、库存/售罄更新、购买飘屏元数据 now survive mixed H5/Mini Program product payload shapes.
- Verification after live product payload hardening: focused Node checks confirm `mapProductItem({ goods_id, goods_pic, goods_name })` produces a usable product card, and WS samples for `product_status_update` with `goods_id`, `product_list` with `goodsList`, and `product_stock` with `goods_id` update讲解状态、货架列表、库存和售罄 state. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Forbidden runtime scan reports only the known CSS class-name false positive `slot-machine-window`; no actionable browser global, H5 player package, JSSDK, or Agora DOM usage was found. Route coverage for 16 core audience routes passes with main package plus `subPackages` expanded. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 00:53:55; generated output contains `getProductId`, `normalizeProductAction`, and `pickProductList`. No `uni`/`vite` watch process remains.
- 2026-06-03 initial/replay product explain compatibility hardening: `useLiveProducts.getProductId` is now the shared canonical product-id resolver for `id`, `productId`, `product_id`, `goodsId`, and `goods_id`. Initial current-product loading, pagination duplicate checks, current-product marking, and manual sold-out preservation now use that resolver instead of raw `id` only. Replay product schedule matching now recognizes the same product-id aliases plus H5 replay video aliases (`video_id`, `replayVideoId`, `replay_video_id`), and replay timeupdate current-product marking uses `mapProductItem(p).id` so raw H5 `goods_id` items still highlight/deactivate correctly.
- Verification after initial/replay product explain hardening: focused source checks confirmed product-id/video-id alias usage in `useLiveProducts`, `useReplayProductSchedule`, and `useLiveReplayPlayback`. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Forbidden runtime scan across broadcast, live-lottery, product/share components, API, platform, services, and `mini-live-socket` reports only the known CSS class-name false positive `slot-machine-window`, with no actionable browser global, H5 player package, JSSDK, or Agora DOM usage. Route coverage for the 16 core audience routes passes with main package plus `subPackages` expanded (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 01:05:46; generated output contains `getProductId`, `getReplayProductId`, `getReplayProductVideoId`, `video_id`, `replay_video_id`, and `mapProductItem(p).id`. No `uni`/`vite` watch process remains.
- 2026-06-03 Mini Program socket leave-event parity hardening: H5's Easemob IM adapter sends a `leave` custom event before leaving/closing a chatroom. The Mini Program fallback path now has the same audience-side behavior in `MiniLiveSocket`: socket `sendEnter()` and new `sendLeave()` both include normalized current-user metadata (`userId`, `customerId`, `nickname`, `nick`, `avatar`), `close()` sends `LEAVE` before disconnecting when the socket is open, and inbound `enter`/`leave` normalization also reads nested `data.nickname` / `data.nick`. `useLiveWebSocket` passes the runtime `userStore.userInfo` into the socket so real-time enter/leave fanout can carry viewer identity instead of anonymous empty payloads.
- Verification after socket leave-event parity hardening: focused Node check simulated an open MiniLiveSocket and confirmed `sendEnter`, `sendLeave`, and `close()` emit types `3`, `4`, and `4` with room/user payload. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Forbidden runtime scan across broadcast, live-lottery, product/share components, API, platform, services, and `mini-live-socket` reports only the known CSS class-name false positive `slot-machine-window`, with no actionable browser global, H5 player package, JSSDK, or Agora DOM usage. Route coverage for the 16 core audience routes passes with main package plus `subPackages` expanded (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 01:15:20; generated dev/build output contains `sendLeave`, `getAudiencePayload`, nested leave nickname normalization, and the socket close-time leave send. No `uni`/`vite` watch process remains.
- 2026-06-03 Mini Program like-event identity parity hardening: H5's IM `sendLike` custom event carries viewer metadata (`userId`, `nickname`, client-side identity) plus the batched like count. The Mini Program socket fallback now sends the same normalized audience payload in `MiniLiveSocket.sendLike(count)`, while preserving the existing batched `count` behavior and HTTP fallback. This prevents like broadcasts from returning as anonymous/self-unidentifiable events, which can cause local optimistic likes to be counted again or lose user attribution in real-time fanout.
- Verification after like-event identity hardening: focused Node check simulated `sendLike(3)` and confirmed type `2`, room id, count `3`, and viewer `userId/customerId/nickname/avatar` are sent in the socket envelope. `npm run build:mp-weixin` passes with only Sass deprecation warnings. Forbidden runtime scan across broadcast, live-lottery, product/share components, API, platform, services, and `mini-live-socket` reports only the known CSS class-name false positive `slot-machine-window`, with no actionable browser global, H5 player package, JSSDK, or Agora DOM usage. Route coverage for the 16 core audience routes passes with main package plus `subPackages` expanded (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 01:21:26; generated dev/build output contains `sendLike(count)` with `...this.getAudiencePayload()` and batched count normalization. No `uni`/`vite` watch process remains.
- 2026-06-03 Mini Program like replay-count parity hardening: socket normalization now separates live-room like totals from batched like deltas. H5/Mini socket payload `count` is treated as `likeDelta`, while `totalLikes` / `likeCount` remain the only total-count aliases. `useLiveWsMessageHandler` applies the server total when present and otherwise increments by a positive delta. This prevents another viewer's `count: 3` batched like event from being misread as total `3` or degraded to a single local increment.
- Verification after like replay-count hardening: focused Node checks covered numeric `type: 2, data.count: 3`, named `event: "like", data.likeCount: 88, count: 3`, and the handler path from `likeCount=10` to `13` and then `88`. `npm run build:mp-weixin` passed with only Sass deprecation warnings. Forbidden runtime scan across broadcast, live-lottery, product/share components, API, platform, services, and `mini-live-socket` still reports only the known CSS class-name false positive `slot-machine-window`. Route coverage for the 16 core audience routes passes with main package plus `subPackages` expanded (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 01:31:54, and generated dev/build output contains `likeDelta` plus the updated fallback increment. No `uni`/`vite` watch process remains. The remaining full-verification boundary is unchanged: a currently live room with Mini Program-playable pull URL is still required before declaring live-player playback, live barrage, live lottery, and watch reward fully verified on device.
- 2026-06-03 replay barrage field-alias hardening: the Mini Program live room now normalizes H5-compatible replay-comment aliases across history loading, outgoing comment payloads, HTTP fallback, and realtime WS callbacks. `useLiveComments` and `useLiveWsMessageHandler` now accept `comment_id`, `msg_id`, `client_msg_id`, `replay_video_id`, `video_id`, `timeline_seconds`, `play_seconds`, `comment_seconds`, and `comment_time` in addition to the existing camelCase fields. Outgoing replay comments include both camelCase and snake_case video/time fields, so backend parsers on either H5 or Mini Program shape can persist them into the replay timeline.
- Verification after replay barrage alias hardening: focused Node checks proved a snake_case comment-history item maps to `commentId/msgId/timelineSeconds/replayVideoId`, replay `sendMessage()` emits both camelCase and snake_case WS payload fields, and a realtime WS chat event with `comment_id/msg_id/timeline_seconds/replay_video_id` enters `replayCommentTimeline` for the active replay video. `npm run build:mp-weixin` passed with only Sass/Wot Design deprecation warnings. Forbidden runtime scan still reports only the known CSS class false positive `slot-machine-window`, with no actionable browser global, H5 player package, JSSDK, or Agora DOM usage. Route coverage for 16 core audience routes passes (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 01:41:56, and generated dev/build output contains the new replay-comment alias fields. No `uni`/`vite` watch process remains.
- 2026-06-03 watch-reward snake_case compatibility hardening: the Mini Program watch-reward runtime now accepts H5-style task, progress, claim, lifecycle, and broadcast shapes such as `marketing_config.watch_rewards`, `activity_id`, `activity_type`, `claim_status`, `reward_name`, `customer_coupon_id`, `user_progress.activity_watched_sec`, `activity_remaining_sec`, and `customer_name`. The shared lottery-win message helper also recognizes snake_case winner record, activity, reward, and customer fields. Claim success normalization now gives the server claim payload precedence over stale local task data, so result popups use the actual claimed reward/order/coupon values returned by `/h5/*`.
- Verification after watch-reward compatibility hardening: focused Node checks covered snake_case live-detail watch-reward entry rendering, snake_case claim result removal/result popup, snake_case watch-reward broadcast float/comment message, and self win notification message insertion. `npm run build:mp-weixin` passed with only Sass/Wot Design deprecation warnings. Forbidden runtime scan across the audience live-room scope reports only the known CSS class-name false positive `slot-machine-window`, with no actionable browser global, H5 player package, JSSDK, or Agora DOM usage. Route coverage for 16 core audience routes passes (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 01:54:03, and generated dev/build output contains `activity_id`, `watch_rewards`, `reward_name`, `customer_coupon_id`, `record_id`, `customer_name`, `activity_type`, and `firstValue`. No `dev:mp-weixin` watch process remains.
- 2026-06-03 Mini Program replay resume playback hardening: `useLiveVideoRuntime` now receives `isReplay` and only blocks `resumeVideoPlayback()` for ended non-replay live rooms (`pushStatus === 2 && !isReplay`). This fixes ended-live rooms that legitimately fall back into replay playback: the user tap / lifecycle resume path can now restart the native replay `<video>` even while the original live status remains ended. The same runtime now prefers the Mini Program `video` context for replay and `live-player` for active live playback, avoiding replay resume attempts being sent to the wrong native component.
- Verification after replay resume hardening: focused Node checks proved `pushStatus=2 + isReplay=true` calls the `video` context, `pushStatus=2 + isReplay=false` remains blocked, and active live playback prefers `live-player`. `npm run build:mp-weixin` passed with only Sass/Wot Design deprecation warnings. Forbidden runtime scan still reports only known CSS class-name false positives (`slot-machine-window` in lottery components), with no actionable browser global, H5 player package, JSSDK, Agora DOM, upload Blob/XMLHttpRequest, or `fetch(` usage in the viewer live-room scope. Route coverage for 16 core audience routes passes (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 02:05:46, and generated dev/build output contains the replay-aware ended-state guard plus `tryPlayNativeContext(!!isReplay.value)`. No `dev:mp-weixin` watch process remains.
- 2026-06-03 ended-status watcher replay guard hardening: `pages/broadcast/entry.vue` no longer lets the `pushStatus` watcher pause native media and clear `videoUrl` when the room has already switched into replay playback (`isReplay && !isScheduleWarmupMode`). This preserves ended-live-to-replay fallback playback after later status refreshes, while keeping the original live-ended stop behavior for non-replay live rooms and schedule warmup states.
- Verification after the watcher guard: focused Node checks covered ended live, ended replay fallback, warmup ended, already-ended, and non-ended transitions. `npm run build:mp-weixin` passed with only Sass/Wot Design deprecation warnings. Forbidden runtime scan across broadcast, live-lottery, lottery-preview, product/share components, API, platform, services, and socket utilities reports only known CSS class-name false positives (`slot-machine-window`), with no actionable browser global, H5 player package, JSSDK, Agora DOM, upload Blob/XMLHttpRequest, or `fetch(` usage in the viewer live-room scope. Route coverage for 16 core audience routes passes (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin/pages/broadcast/entry.js` at 2026-06-03 02:16:07; generated dev output contains `if (isReplay.value && !isScheduleWarmupMode)`, and generated build output compresses the same guard to `!Vt.value || Ke`.
- 2026-06-03 replay simulated-order payload compatibility hardening: `useReplaySimOrders` now normalizes `/h5/live/replaySimMessages` responses before timeline consumption. It accepts array, `list`, `records`, `rows`, `items`, `messages`, `simMessages`, `sim_messages`, `orderList`, `order_list`, and nested `data`/`result` wrappers, then maps H5-style snake_case fields such as `trigger_at_sec`, `play_time`, `product_id`, `goods_id`, `customer_name`, `product_name`, `goods_pic`, `buy_count`, and `notice_text` into the canonical `triggerAtSec`, `productId`, `customerName`, `productName`, `productImage`, `quantity`, and `noticeText` shape consumed by replay purchase飘屏. Time strings like `01:05` are converted to seconds. This keeps录播模拟购买提醒 working when the backend returns common H5 field shapes instead of the narrow camelCase shape.
- Verification after replay simulated-order hardening: focused Node checks covered snake_case list payloads, nested `data.records` with nested product/customer objects and `MM:SS` time strings, invalid trigger filtering, and chronological sorting. `npm run build:mp-weixin` passed with only Sass/Wot Design deprecation warnings. Forbidden runtime scan still reports only known CSS class-name false positives (`slot-machine-window`), with no actionable browser global, H5 player package, JSSDK, Agora DOM, upload Blob/XMLHttpRequest, or `fetch(` usage in the viewer live-room scope. Route coverage for 16 core audience routes passes (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin/pages/broadcast/composables/useReplaySimOrders.js` at 2026-06-03 02:27:56, and generated dev/build output contains `normalizeReplaySimMessages`, `trigger_at_sec`, `customer_name`, `goods_pic`, and `play_time`.
- 2026-06-03 Mini Program comment HTTP fallback parity hardening: `api/live.buildLiveCommentPayload` now emits both camelCase and snake_case flat fields for `msgId/msg_id`, `clientMsgId/client_msg_id`, `timelineSeconds/timeline_seconds`, `replayVideoId/replay_video_id`, `videoId/video_id`, and `termId/term_id`, while preserving the original nested `data` payload. The fallback also mirrors `videoId` into `replayVideoId` and vice versa in both naming styles. This protects live/replay barrage sending when WebSocket is reconnecting or unavailable and the H5 `/h5/live/comment` parser expects flat snake_case metadata for replay timeline persistence.
- Verification after comment fallback parity hardening: real H5 smoke-room inspection showed `wusrc6dh3wxd` currently returns `pushStatus: 2`, `liveStatusText: "回放"`, `replays[0].id: 574`, `/h5/live/commentHistory` returns `data[]`, and comment metadata may be nested under `data.commentId`, `data.replayVideoId`, and `data.timelineSeconds`; current comment history mapping already reads that shape. Focused payload checks proved snake_case and camelCase inputs both produce dual flat aliases plus replay/video fallback ids. `npm run build:mp-weixin` passed with only Sass/Wot Design deprecation warnings. Forbidden runtime scan still reports only known CSS class-name false positives (`slot-machine-window`), with no actionable browser global, H5 player package, JSSDK, Agora DOM, upload Blob/XMLHttpRequest, or `fetch(` usage in the viewer live-room scope. Route coverage for 16 core audience routes passes (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin/api/live.js` at 2026-06-03 02:36:31, and generated dev/build output contains the dual comment metadata aliases.
- 2026-06-03 replay/live-detail field-shape compatibility hardening: `useLiveEntryInitializer` now normalizes H5 replay and detail fields before they enter playback state. Replay video lists accept `replayVideos`, `replay_videos`, `replayList`, `replay_list`, `replays`, single `replayVideo`, and single `replay_video`; each item maps `video_id`, `replay_video_id`, `play_url`, `m3u8_url`, `cover_image`, `duration_sec`, `term_id`, `start_time`, `estimated_end_time`, `loop_play`, `term_loop_play`, and string/JSON `quick_replies` into the camelCase shape consumed by the copied H5 replay player, product schedule, progress report, and quick-reply UI. Null/bad replay list entries are filtered instead of breaking room initialization.
- The same pass also normalizes top-level detail fields used by the live/replay branch decisions: `push_status` / `live_status` / `status`, `has_replay`, `room_id`, `tenant_id`, `customer_id`, `watch_by_day`, `current_term_id`, `room_name`, `cover_image`, `online_count`, `like_count`, `live_status_text`, `chat_bg_image`, and `start_time`. `replayFirstVideo` normalization now accepts snake_case video URL/id/term/progress fields, sign-in config accepts `sign_config` / `sign_enabled` / `sign_fields`, and replay resume accepts `resume_video_id`, `resume_video_index`, and `last_position`. This closes the log-observed failure mode where Mini Program initialization could see `replayVideosList.length = 0` even when an H5-shaped replay payload existed.
- Verification after replay/live-detail field-shape hardening: focused Node checks covered real `replays[0]` shape, snake_case `replay_videos`, null replay list entries, JSON quick replies, snake_case sign config, snake_case resume state, and snake_case `replayFirstVideo` payload. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Forbidden runtime scan across broadcast, live-lottery, lottery-preview, product/share components, API, platform, services, and socket utilities still reports only known CSS class-name false positives (`slot-machine-window`), with no actionable browser global, H5 player package, JSSDK, Agora DOM, upload Blob/XMLHttpRequest, or `fetch(` usage in the viewer live-room scope. Route coverage for 16 core audience routes passes (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at 2026-06-03 02:58:11, and generated dev/build output contains `getDetailPushStatus`, `push_status`, `has_replay`, `replay_videos`, `sign_enabled`, `resume_video_id`, `play_url`, and `elapsed_seconds`.
- 2026-06-03 live-room WXSS selector compatibility hardening: H5-copied live-room styles still contained Mini Program-unsafe selector patterns in audience live-room surfaces, including `::-webkit-scrollbar`, `::before`, `::after`, `scrollbar-width`, and `-ms-overflow-style`. `product-card` now renders the H5讲解卡片扫光层 and 售罄 side lines as real Mini Program nodes (`explain-goods-bottom-highlight`, `sellout-label-line`) while keeping the H5 card class names. `product-list` now uses a real `product-scroll-content` wrapper for popup shelf padding instead of styling generated scroll-view internals. Broadcast portrait/landscape chat areas, the product shelf wrapper, and watch-reward modal sources no longer emit scrollbar pseudo selectors.
- Verification after WXSS selector hardening: source scan across `pages/broadcast`, `components/product-card.vue`, `components/product-list.vue`, `components/live-lottery`, and `pages/lottery-preview` finds no remaining `::-webkit-scrollbar`, `::before`, `::after`, `scrollbar-width`, or `-ms-overflow-style` patterns. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Generated build and dev WXSS checks for `pages/broadcast/entry`, portrait/landscape stages, product shelf, `components/product-card`, `components/product-list`, and watch-reward modal report no Mini Program-unsafe pseudo/scrollbar selectors. Forbidden runtime scan still reports only the known CSS class-name false positive `slot-machine-window`. Route coverage for 16 core audience routes still passes (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated dev output contains `explain-goods-bottom-highlight`, `sellout-label-line`, and `product-scroll-content`, with no `uni`/`vite`/`node` watch process remaining.
- 2026-06-03 broadcast parity audit: file-level comparison against sibling `../live_h5/src/pages/broadcast` shows every non-test `.vue`, `.js`, and `.scss` broadcast file exists in `uniapp-src/src/pages/broadcast`; the Mini Program side only adds `utils/domain-ready.js` as an adapter helper. Endpoint extraction across H5 broadcast plus H5 live/complaint APIs versus Mini Program broadcast plus Mini Program live/marketing/complaint APIs shows no missing `/h5/*` live-room endpoint; Mini Program additionally has `/h5/live/imToken` for the current IM helper path. Class-token comparison shows remaining differences are expected Mini Program runtime substitutions such as `live-player`, `video`, native context helpers, `/static` asset paths, and real-node replacements for H5 pseudo-elements.
- Verification after parity audit: forbidden browser-runtime scan across broadcast, live-lottery, live-sign-in, live-report-popup, share-popup, API, platform, services, `mini-live-socket`, `ws-envelope`, and `videoPlay` reports only the CSS class-name false positive `slot-machine-window`; no actionable `window`, `document`, storage global, `navigator`, `hls.js`, `flv.js`, `weixin-js-sdk`, Agora DOM, Blob/XHR/fetch upload path, or browser DOM insertion remains in the viewer live-room source. Complaint/refund uploads route through `platform/weixin/file.js` and `uni.uploadFile`/native PUT adapter. `npm run build:mp-weixin` still passes after the audit, with only Sass/Wot Design deprecation warnings.
- 2026-06-03 lottery/modal parity re-audit: H5 `CommentLotteryModals` was inspected and confirmed to be a thin wrapper over `comment-prize-rule-modal` plus `CommentPrizeConfirmModal`; the Mini Program split implementation keeps the same visible modal behavior while using direct imports instead of `defineAsyncComponent`. The live-room-used lottery components (`CommentLotteryPasswordModal`, `CommentSlotLotteryModals`, `CommentLotteryResultModals`, `SlotLotteryModals`, `WechatLotteryModals`, `WechatLotteryAwardsUserModal`, `WechatLotteryResultModals`, `WatchDurationRewardModal`, `CouponReceiveModals`, and `modal-data.js`) were compared against H5 source. Differences are Mini Program-safe class/path/event adaptations: `/static` asset paths, real classes replacing tag/id selectors, and forwarded one-click comment text for comment-lottery participation.
- Verification after lottery/modal parity re-audit: `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Broadcast source coverage remains complete (`77` H5 runtime files, `78` Mini Program files including adapter-only `utils/domain-ready.js`). Visual class coverage reports `0` missing H5 broadcast classes in Mini Program broadcast/shared component sources. Shared live-room component class coverage for `center-popup`, `live-report-popup`, `live-sign-in`, `product-buy-popup`, `product-card`, `product-list`, `share-popup`, and `comment-prize-rule-modal` also reports no missing H5 classes. Forbidden runtime scan across broadcast, live-lottery, live-sign-in, live-report-popup, share-popup, API, platform, services, `mini-live-socket`, `ws-envelope`, and `videoPlay` reports only the known CSS class-name false positive `slot-machine-window`. Core route coverage passes for `16` audience routes with `routeCount: 196`. Generated build WXSS checks for broadcast/player/product/lottery surfaces report no Mini Program-unsafe pseudo/scrollbar/deep selectors.
- 2026-06-03 core-stage parity audit: `LivePortraitStage`, `LiveLandscapeStage`, `LiveChatBar`, and `LiveProductShelf` were diffed against H5. Remaining structural differences are Mini Program platform substitutions or previous parity fixes: native `live-player`/`video`, Mini Program media contexts, error/retry overlays, `/static` asset paths, share `roomCode`/tenant/distributor passthrough, selector-query quick-reply positioning instead of DOM measurements, and real nodes/classes replacing H5 pseudo/id/tag selectors. The apparent extra fire icons in anchor/viewer metadata are replacements for H5 `::before` pseudo-elements, not a redesigned UI. Non-essential `console.log("[Live] live-player statechange")` debug output was removed from portrait and landscape stage handlers while preserving error logs for real-device playback troubleshooting.
- Verification after core-stage parity audit: `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Forbidden runtime scan still reports only the known CSS class-name false positive `slot-machine-window`. Core route coverage passes (`routeCount: 196`). Source/build/dev scans confirm `live-player statechange` debug logs are absent. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` at `2026-06-03 03:46:43`, and no `uni`/`vite`/`node` watch process remains.
- 2026-06-03 test credential cleanup: the Mini Program live-entry bootstrap no longer contains the temporary hardcoded smoke-room token or `wusrc6dh3wxd` room fallback. Explicit debug entry still exists through page params (`wx_token`, `roomCode`, etc.) or local Mini Program storage keys (`debug_live_room_code`, `debug_live_wx_token`, `debug_live_tenant_id`, `debug_live_type`, `debug_live_tc`), but no test credential is embedded in source or generated packages.
- Verification after test credential cleanup: `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Source, `dist/build/mp-weixin`, and refreshed `dist/dev/mp-weixin` scans find no `eyJhbGci`, old smoke-token fragments, `HARDCODED_TEST_LIVE_ENTRY`, or `wusrc6dh3wxd`. Forbidden runtime scan still reports only the known CSS class-name false positive `slot-machine-window`. Core route coverage passes (`routeCount: 196`). `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin/pages/broadcast/composables/useLiveEntryBootstrap.js` at `2026-06-03 03:54:29`, and no `uni`/`vite`/`node` watch process remains.
- 2026-06-03 live pull-source alias hardening: Mini Program live playback now extracts Mini Program-playable candidates from H5-compatible stream arrays as well as flat fields. `getMiniProgramLiveCandidates` descends through `pullStreams`, `pull_streams`, `streamList`, `stream_list`, `streams`, `liveStreams`, and `live_streams`, and recognizes nested `rtmp_url`, `flv_url`, `hls_url`, `pull_*`, `play_url`, `stream_url`, and `url`. `normalizePullStreams` accepts the same list aliases plus `quality_name` / `definition`, carries `backupRtmpUrl`, and selects RTMP/FLV for native `live-player` with HLS/video as fallback. `live-status-snapshot` now treats snake_case status and stream fields as valid live-status payloads, so WS/status-poll updates cannot be ignored just because the backend uses H5-shaped names.
- Verification after pull-source alias hardening: focused Node sanity check confirmed a `pull_streams` payload containing `rtmp_url`, `flv_url`, `hls_url`, and `play_url` becomes ordered RTMP `live-player`, FLV `live-player`, HLS `video`, then the secondary FLV candidate. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Forbidden runtime scan across broadcast, live-lottery, live-sign-in, live-report-popup, share-popup, API, platform, services, `mini-live-socket`, `ws-envelope`, and `videoPlay` still reports only the known CSS class-name false positive `slot-machine-window`; no browser global, H5 player package, JSSDK, Agora DOM, Blob/XHR/fetch upload path, or browser DOM insertion is present. Core audience route coverage passes for 16 routes with `routeCount: 196`. Source/build/dev hardcoded-credential scans find no old token fragments, `HARDCODED_TEST_LIVE_ENTRY`, `applyHardcodedTestLiveEntry`, or `wusrc6dh3wxd`.
- Remaining verification boundary is unchanged: the supplied smoke room can validate replay fallback and UI/runtime code, but it is not currently live. A room that is actively pushing (`pushStatus: 1`) and returns a Mini Program-playable RTMP/FLV/HLS pull URL is still required before declaring live-player playback, realtime barrage, live lottery, and live watch-reward fully verified on device.
- 2026-06-03 live status and socket envelope hardening: `live-status-snapshot` now applies snake_case/camelCase status payloads before playback decisions and allows a room currently in replay fallback to restore into live playback when a WS/status-poll snapshot reports `pushStatus: 1` plus a resolved RTMP/FLV/HLS pull URL. This fixes the previous replay guard risk where a room could stay on replay after the backend resumed live streaming. `mini-live-socket` now also unwraps H5/IM-style `customEvent` / `customExts.payload` envelopes, preserves the custom event as `event`/`name`/`type`, normalizes snake_case like/viewer fields, and filters cross-room messages by `roomId`/`liveId` before dispatching to broadcast handlers. This protects comment lottery, normal lottery, watch reward, product/status broadcasts, live status updates, and barrage paths from silent drops caused by H5 envelope shape differences.
- Verification after live status/socket envelope hardening: a focused Mini Program socket adapter check confirmed `customEvent: "comment_lottery"` with JSON `customExts.payload` becomes handler-visible `type: "comment_lottery"` / `action: "openPrize"`, snake_case `like_count` / `like_delta` becomes `type: "like"` with total/delta values, numeric `type: 30` plus snake_case live status remains `live_status_update`, and a mismatched `roomId` message is discarded. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Forbidden runtime scan across the viewer live-room scope still reports only the known CSS class-name false positive `slot-machine-window`; no actionable browser global, H5 player package, JSSDK, Agora DOM, Blob/XHR/fetch upload path, or browser DOM insertion is present. Core audience route coverage passes for 16 routes with `routeCount: 196`. Source/build/dev hardcoded-credential scans find no old token fragments, smoke-room token, `HARDCODED_TEST_LIVE_ENTRY`, `applyHardcodedTestLiveEntry`, or `wusrc6dh3wxd`. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated dev output contains `customEvent`, `customExts`, `getMessageRoomId`, and `canRestoreLiveFromReplay`.
- Remaining verification boundary after this pass: source/build/dev checks prove the adapter and state-machine paths are present, but they do not prove live media playback or live marketing events against production infrastructure. Final completion still requires a currently live room with `pushStatus: 1`, a Mini Program-playable RTMP/FLV/HLS pull URL, socket domain access, valid audience auth, and enabled lottery/watch-reward data so WeChat DevTools plus a real device can verify live-player playback, realtime barrage send/display, likes, normal lottery, comment lottery, watch reward, product shelf events, report/share, and replay/live switching. The known room `wusrc6dh3wxd` remains replay-only and cannot close that proof gap.
- 2026-06-03 playback/interaction source audit follow-up: the supplied test link was rechecked against real `/h5/live/detail` and `/h5/live/replayFirstVideo` responses. `wusrc6dh3wxd` currently returns `pushStatus: 2`, `liveStatusText: "回放"`, and replay data under `replays[0]`; `/h5/live/replayFirstVideo` returns `hasVideo: false`. Current source and refreshed `dist/dev/mp-weixin` both contain `normalizeReplayVideos(d)` plus the `detail.replays` fallback, so this H5 field shape is covered by the Mini Program replay `<video>` path. The same audit confirmed replay mode never selects `live-player`, live mode only selects `live-player` for RTMP/FLV-style URLs, and `getLiveVideoElement()` is a Mini Program no-op so copied H5 DOM replay-reuse code cannot run in mp-weixin.
- 2026-06-03 log-noise cleanup for real-device playback debugging: removed non-essential `console.log` output from `live-sign-in` validation and ordinary WebSocket connect/sign-key paths, and removed the marketing guard debug warning. Playback errors, WebSocket errors, business failures, and playback fallback warnings remain available so real-device failures still surface actionable diagnostics.
- Verification after playback/interaction source audit follow-up: `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Forbidden runtime scan across broadcast, live-lottery, live-sign-in, live-report-popup, share-popup, API, platform, services, `mini-live-socket`, `ws-envelope`, and `videoPlay` reports only the known CSS class-name false positive `slot-machine-window`; no actionable browser global, H5 player package, JSSDK, Agora DOM, Blob/XHR/fetch upload path, or browser DOM insertion is present. Core audience route coverage passes for 16 routes with `routeCount: 196`, and route normalization checks pass for `scene=live_id:123`, `live_id=123`, `roomCode=xxx`, and `replay=1`. Source/build/dev hardcoded-credential scans find no smoke-room token, old token fragments, `HARDCODED_TEST_LIVE_ENTRY`, `applyHardcodedTestLiveEntry`, or `wusrc6dh3wxd`. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` at `2026-06-03 04:55:44`; generated output contains the `replays` replay-list fallback and no longer contains the removed signup/marketing/WS debug logs.
- Remaining verification boundary is still open: the current evidence proves source and generated package readiness for replay fallback, route compatibility, socket-envelope handling, and non-payment interaction paths, but it does not prove an actively pushing room. Completion still requires a live `pushStatus: 1` room returning a Mini Program-playable RTMP/FLV/HLS URL, enabled lottery/watch-reward/comment-lottery data, valid auth, and WeChat DevTools plus real-device tests for live-player playback, realtime barrage send/display, likes, lottery, watch reward, product shelf pushes, report/share, and replay/live transition.

- 2026-06-03 05:21 follow-up verification for the explicit goal "pixel-level H5 live-room migration with all non-payment viewer functions usable": file-level comparison against `../live_h5/src/pages/broadcast` still shows all `77` H5 broadcast `.vue` / `.js` / `.scss` runtime files present in `uniapp-src/src/pages/broadcast`; Mini Program adds only adapter helper `utils/domain-ready.js`. `normalizeLiveRouteOptions` now also derives `isReplay` from `replay=1`, `mode=replay`, or `liveType=replay`, and `isReplayEntry` consumes that flag. Route checks pass for broadcast entry/replay, legacy live vertical/horizontal, agent share-live redirect routes, center, order/pay/receipt/refund, address, report, prize record, and invitation record (`routeCount: 196`). Normalization checks pass for `scene=live_id:123`, `live_id=123`, `roomCode=xxx`, `replay=1`, `mode=replay`, and `liveType=replay`.
- Verification after the same pass: `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin` at `2026-06-03 05:21:05`; generated dev output contains the replay flag derivation, the `detail.replays` fallback, and enter-failure cleanup (`sessionId.value = ""` plus `setEnterTimestamp(0)`). Source/build/dev scans find no hardcoded smoke token, old token fragments, `HARDCODED_TEST_LIVE_ENTRY`, `applyHardcodedTestLiveEntry`, or `wusrc6dh3wxd`. Generated dev scans also find no old `defineAsyncComponent`, `videoPlayer is not defined`, `handlePageHide is not defined`, H5 player/JSSDK imports, or actionable browser globals in the live-room viewer scope; remaining scan hits are the known CSS class-name false positive `slot-machine-window`.
- Current test-data boundary: the supplied test room `wusrc6dh3wxd` has been observed as replay-only (`pushStatus: 2`, `liveStatusText: "回放"`, replay data under `replays[0]`, and no usable live stream from the live endpoints). It is suitable for validating replay fallback through Mini Program `<video>`, copied replay UI, route/auth setup, and non-live UI surfaces. It cannot close the live-player proof gap for active live playback, realtime barrage over production socket, live lottery, live watch reward, or live product push events. Declaring this goal complete still requires a currently live `pushStatus: 1` room with Mini Program-playable RTMP/FLV/HLS, enabled lottery/watch-reward/comment-lottery data, valid audience auth, and WeChat DevTools plus real-device acceptance.

- 2026-06-03 05:34 personal-center H5 page restoration: `/pages/center/index` no longer redirects into the legacy Mini Program tabbar `/pages/user/index/index`. The Mini Program center page now uses the H5 `center-page` / `center-main` / `profile-row` / `profile-avatar` / `profile-name` / `profile-more-card` / `back-pill` page structure with `center-section-card` and `live-mini-window`, reads user data from `/h5/user/center` with cached H5 customer/user-store fallback, and reads order/refund badges from `/h5/order/unreadStats` plus `/h5/refund/unreadStats`. The H5 live-room module set is preserved and extended with the H5 center-popup address entry: orders, refund/售后, prize record, distributor invitation record,收货地址, complaint/report, and return-to-live all navigate to real migrated pages instead of the legacy tabbar shell or placeholder text.
- Verification after personal-center restoration: center class-token coverage against `../live_h5/src/pages/center/index.vue` now has no missing visual classes; remaining token differences are script-only (`redirectTo`, `token`). `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `uniapp-src/dist/dev/mp-weixin/pages/center/index.*` at `2026-06-03 05:34:15`; generated dev output contains `center-page`, `profile-row`, `profile-avatar`, `profile-name`, `收货地址`, `getCenter`, and `/h5/user/center`, and no longer contains `正在打开个人中心` or `switchTab` center-stub output. Route checks pass for `/pages/center/index`, order list, address index, report type, prize record, and invitation record.

- 2026-06-03 05:45 invitation page H5 visual-shell parity hardening: `/pages/invitation/index` kept the Mini Program native layered-image implementation for invitation rendering, QR generation, copy-link, save-QR, and native share hooks, but restored the H5 page shell classes and top navigation structure (`inv-nav`, `inv-nav__back`, `inv-nav__close`, `inv-nav__center`, `inv-nav__title`, `inv-nav__sub`, `inv-nav__more`, `inv-nav__more-dot`). This moves the share/invitation viewer loop closer to the H5 visual source without reintroducing browser-only `document.createElement("canvas")`, `Image`, or H5 canvas export.
- Verification after invitation shell hardening: invitation page class-token audit reports only the H5 `theme` canvas-gradient config as an actionable remaining token difference; the missing top-nav visual classes are now present. `npm run build:mp-weixin` generated `dist/build/mp-weixin/pages/invitation/index.*` at `2026-06-03 05:43:27` with `inv-nav` WXML/WXSS present and no browser-global hits. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin/pages/invitation/index.*` at `2026-06-03 05:45:28`, and generated dev output contains the same H5 nav structure. Forbidden runtime scan across invitation, center, broadcast, share, API, services, and platform scopes still reports no actionable browser global or H5 player/JSSDK dependency.

- 2026-06-03 06:19 replay playback fallback hardening for the supplied smoke room: real `/h5/live/detail` for `wusrc6dh3wxd` returns `pushStatus: 2`, `liveStatusText: "回放"`, and a playable replay only under `replays[0].videoUrl`; `/h5/live/replayFirstVideo` returns `hasVideo: false`. The Mini Program replay initializer now treats replay lists with no `startTime` / `estimatedEndTime` as unscheduled replay assets and directly calls `playReplayVideoByIndex()` instead of marking the room ended. The `pushStatus === 2` watcher was also narrowed so the initialization window with a replay list but no assigned `videoUrl` does not clear the pending replay source, while true live-end cleanup still runs once a live source exists.
- Verification after replay playback fallback hardening: focused Node simulation of the observed `wusrc6dh3wxd` payload resolves one replay item and `shouldPlayUnscheduledReplayList: true`. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Refreshed `dist/dev/mp-weixin` contains `hasReplay.value && replayVideosList.value.length > 0 && !videoUrl.value`, `shouldPlayUnscheduledReplayList`, and `playUnscheduledReplayVideo`; build output regenerated at `2026-06-03 06:19:13`. Forbidden runtime scan across broadcast, live-lottery, live-sign-in, live-report-popup, share-popup, live API, `videoPlay`, `live-route`, and `mini-live-socket` reports only the known CSS class-name false positive `slot-machine-window`, with no actionable browser global, H5 player package, JSSDK, Agora DOM, Blob/XHR/fetch upload path, or browser DOM insertion in the viewer live-room scope.
- 2026-06-03 12:31 H5 API HTML-response guard and dev-package verification: direct backend checks confirmed the real JSON endpoint shape is `/api/h5/*`; using a base URL that drops `/api` can return an H5 HTML page instead of API JSON. `h5Request` now detects HTML responses and rejects with `H5 API返回了页面HTML，请检查h5_api_url是否指向后端/api`, preserving a clear failure mode for misconfigured Mini Program API base URLs instead of letting live detail, replay, lottery, or comment initialization consume page markup.
- Verification after the HTML-response guard: `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Static scan across broadcast, live-lottery, lottery-preview, API, platform, and services scopes reports no actionable `window`, `document`, browser storage globals, `navigator`, H5 player packages, JSSDK, DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` usage after filtering known false positives. Core route coverage passes in both source `pages.json` and generated `dist/dev/mp-weixin/app.json` for broadcast entry/replay, legacy live vertical/horizontal, center, prize record, invitation record, and report. `dist/dev/mp-weixin` contains both the unscheduled `detail.replays` replay fallback and the new HTML-response guard, so the package currently imported into WeChat DevTools is up to date for these fixes.
- 2026-06-03 15:02 lottery field-shape compatibility hardening: ordinary lottery and comment-lottery runtimes now normalize H5 backend `snake_case` fields in addition to camelCase fields. Covered paths include activity/prize ids, active prize, prize lists, winner/participant lists, record ids, product/reward names and images, countdown/draw ids, display password text, current-user win records, participants URL, and win-record updates. This keeps the copied H5 visual components unchanged while making the Mini Program runtime accept `/h5/*` and WebSocket payloads shaped as `activity_id`, `prize_id`, `winner_list`, `record_list`, `display_password_text`, `current_user`, and related aliases.
- Verification after lottery field-shape hardening: `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `dist/dev/mp-weixin/pages/broadcast/composables/useLiveNormalLottery.js` and `useLiveCommentLottery.js` contain the new alias normalization. Source scan of the two lottery runtimes reports no browser globals or debug logs. Core route coverage still passes in source and generated dev app manifests (`routeCount: 196`), and forbidden runtime scan across broadcast, live-lottery, lottery-preview, API, platform, and services scopes reports no actionable browser global, H5 player package, JSSDK, DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` usage.
- 2026-06-03 07:27 live-room WS identity and share-link hardening: the Mini Program socket fallback now emits both camelCase and snake_case room/user identity fields (`roomId/room_id`, `userId/user_id`, `customerId/customer_id`, `nickname/nick/userName/user_name/customerName/customer_name`, avatar aliases) for chat-adjacent enter/leave/like events. Inbound chat/user/product/order handling now also reads snake_case aliases for self-message detection, optimistic barrage upgrades, mute/block targeting, paid-order dedupe, product notice names/images, and audience nick/avatar display. This preserves the copied H5 UI while reducing production WS payload-shape drift for barrage, likes, buy notices, and marketing events. `share-popup` no longer fabricates `https://s.example.com/*` short links; the legacy short-link helper now copies the resolved real Mini Program/H5 distributor share link instead.
- Verification after WS identity/share-link hardening: `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Route coverage passes with main package and subPackages expanded (`routeCount: 196`) for broadcast entry/replay and legacy live vertical/horizontal paths. Forbidden runtime scan across broadcast, live sign/report/share components, `mini-live-socket`, `ws-envelope`, API, platform, and services reports no actionable browser global, H5 player package, JSSDK, Agora DOM, `fetch`, `Blob`, or `XMLHttpRequest` usage. Focused Node checks confirm `MiniLiveSocket.getAudiencePayload()` emits the dual identity aliases and a snake_case chat event (`user_id`, `customer_name`, `msg_id`, `comment_id`, `head_img`) upgrades the local optimistic barrage correctly. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` at `2026-06-03 07:27:56`; generated dev output contains the updated socket/share code.

- 2026-06-03 08:01 legacy replay-route normalization hardening: `normalizeLiveRouteOptions` now normalizes `live_type` into `liveType`, and when an old link only carries `mode=replay` it derives `liveType: "replay"` plus `isReplay: true`. `buildBroadcastEntryUrl` now emits `replay=1` whenever replay intent is derived from `mode`, `liveType`, or `live_type`, so legacy Mini Program live routes and H5-style links do not lose replay state when redirected into `/pages/broadcast/entry`.
- Verification after replay-route normalization hardening: focused route checks pass for `scene=live_id:123`, `live_id=123`, `roomCode=xxx`, `replay=1`, `mode=replay`, `liveType=replay`, and `live_type=replay`; `mode=replay&liveId=123` now produces `/pages/broadcast/entry?liveId=123&mode=replay&replay=1&liveType=replay`. Source, `dist/dev/mp-weixin/app.json`, and `dist/build/mp-weixin/app.json` route scans all pass with `routeCount: 196` for broadcast entry/replay, legacy live vertical/horizontal, agent share-live redirect, center, prize record, invitation record, and report routes. The refined forbidden-runtime scan covers 132 viewer live-room files and reports no actionable browser global, H5 player package, JSSDK, Agora DOM, `fetch`, `Blob`, or `XMLHttpRequest` usage. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` at `2026-06-03 07:59:40`; generated build/dev output contains the `mode === "replay" ? "replay" : ""` fallback and generated `replay=1` route parameter. No real `uni`/`vite`/`mp-weixin` process remains after verification.

- 2026-06-03 08:25 H5 login-skip return-path hardening: the login page's "暂不登录" action now routes through `redirectAfterH5LoginSkipped()` instead of always returning to `/pages/index/index`. When the saved H5 auth context points at `/pages/broadcast/entry` or `/pages/broadcast/replay`, it relaunches that live-room route with `loginSkipped=1`; `runLiveEntryBootstrap()` recognizes and strips that marker before login interception so the user is not immediately redirected back into the login page. Non-live skip fallback now returns to `/pages/index/index?loginSkipped=1` instead of the H5 center page to avoid an unauthenticated center/login loop.
- Verification after login-skip hardening: focused source checks confirmed `redirectAfterSkippedH5Login(this.loginContext)`, `redirectAfterH5LoginSkipped`, the broadcast `loginSkipped/login_skipped` guard, and the non-live home fallback. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `dist/build/mp-weixin` and `dist/dev/mp-weixin` contain the updated login page, H5 auth service, and broadcast bootstrap composable; generated output includes `loginSkipped`, `/pages/index/index?loginSkipped=1`, and the `loginSkipped/login_skipped` strip path. Static scan of the changed live/auth/login scope reports no actionable browser globals or H5 player/JSSDK dependencies. The per-run `npm run dev:mp-weixin` watch process started for this verification was cleaned up; unrelated historical watch processes were left untouched.
- Remaining verification boundary is unchanged: these checks prove source and generated package readiness for the auth/route/playback/interaction code paths, but final completion still requires a currently live `pushStatus: 1` room with a Mini Program-playable pull URL and enabled barrage/lottery/watch-reward data so WeChat DevTools plus a real device can verify live-player playback, realtime barrage send/display, likes, normal lottery, comment lottery, watch reward, product shelf pushes, report/share, and replay/live switching.

- 2026-06-03 09:02 share invitation and bindId entry hardening: `share-popup` no longer terminates the "生成邀请函" action with an unavailable-page toast. It now writes an invitation payload containing both the resolved H5/distributor link and the Mini Program broadcast path (`miniProgramPath`, `roomCode`, `roomId/liveId`, `tenantId`) before navigating to `/pages/invitation/index`; navigation falls back from `navigateTo` to `redirectTo` with a concrete failure toast only if both fail. `/pages/invitation/index` now uses the H5/distributor link for QR/copy output while using `miniProgramPath` for `onShareAppMessage`, `onShareTimeline`, and fallback return-to-live routing, so Mini Program share cards do not receive an invalid HTTPS path. The Mini Program `bindid` service was also replaced from an unsupported stub with a Mini Program-safe local bindId manager plus `/h5/bind/identity` API support. When a live-room link carries `bindId`/`bind_id` and no local token, `runLiveEntryBootstrap()` now attempts `bindIDManager.smartAutoLogin()` before redirecting to the login page. H5-only cross-domain iframe/image cookie syncing remains explicitly skipped with `mp-weixin-no-cross-domain-*` reasons.
- Verification after share invitation and bindId entry hardening: source checks confirm `api/bind.js` exposes `/h5/bind/identity`, `services/bindid.js` contains `smartAutoLogin()` plus `syncH5AuthSession()`, `useLiveEntryBootstrap` statically imports `bindIDManager`, `share-popup` persists `miniProgramPath`, and invitation sharing uses `shareMiniProgramPath`. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `dist/build/mp-weixin` and refreshed `dist/dev/mp-weixin` both contain `api/bind.js`, `services/bindid.js`, the static `require("../../../services/bindid.js")` bootstrap dependency, `miniProgramPath` in share/invitation output, and no invalid `await "../../../services/bindid.js"` dynamic-import artifact. Focused forbidden-runtime scan across broadcast, share-popup, invitation, bind API/service, H5 auth context, and Mini socket reports no actionable browser global, H5 player package, JSSDK, Agora DOM, `fetch`, `Blob`, or `XMLHttpRequest` usage after filtering known CSS/placeholder false positives. The per-run `npm run dev:mp-weixin` watch process for this verification was cleaned up; unrelated historical watch processes were left untouched.
- Remaining verification boundary is still unchanged: this pass closes another share/auth entry gap that can be proven statically, but it still does not prove active live media or live marketing events. Final completion still requires a currently live `pushStatus: 1` room with Mini Program-playable RTMP/FLV/HLS, valid audience auth, socket domain access, and enabled barrage/normal-lottery/comment-lottery/watch-reward/product-push data for WeChat DevTools plus real-device verification.

- 2026-06-03 15:52 replay-first-video and Mini Program runtime-import hardening: replay initialization now preserves the early `/h5/live/replayFirstVideo` payload as `earlyReplayFirstVideoState.video`, promotes it into `replayVideosList` when detail/replay-list payloads are empty, and emits a `replay_first_video_fallback` debug event. `reuseEarlyReplayFirstVideoIfSame()` now only reuses early state when the current playback URL already matches the target replay video, preventing replay metadata from short-circuiting native `<video>` startup. `useLivePurchase` now statically imports `getProductDetail` from `@/api/product.js` instead of using a runtime `await import()`, which avoids Mini Program dynamic-import runtime failures while keeping the copied H5 purchase flow structure.
- Verification after this pass: `node --check` passes for `useLiveEntryInitializer.js`, `useLivePurchase.js`, `mini-live-socket.js`, `ws-envelope.js`, `useLiveWebSocket.js`, and `useLiveReplayPlayback.js`. File-level comparison against `../live_h5/src/pages/broadcast` still reports all `77` H5 broadcast `.vue` / `.js` / `.scss` runtime files present in `uniapp-src/src/pages/broadcast` with `missing_in_uniapp_src=0`. Route coverage passes when main package and `subPackages` are both expanded for broadcast entry/replay, legacy live vertical/horizontal, and agent share-live redirect routes. Forbidden-runtime scan across broadcast, live-lottery, lottery-preview, live API, `mini-live-socket`, and `ws-envelope` reports `forbidden_hits=0` for unadapted browser globals, H5 player packages, JSSDK, dynamic imports, Blob/XHR/fetch upload paths, or Agora DOM usage. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings, and both `dist/build/mp-weixin` and the currently imported `dist/dev/mp-weixin` contain `replay_first_video_fallback` plus the static product-detail dependency.
- Remaining verification boundary is still open for the active-live claim: the checks above prove the source and generated Mini Program package are ready for replay fallback, route compatibility, socket-envelope normalization, and non-payment interaction code paths that can be verified statically. They still do not prove live-player playback, realtime barrage send/display, likes, lottery, watch reward, or live product push on an actively pushing room. Final completion still requires a valid `pushStatus: 1` live room with a Mini Program-playable pull URL, enabled live marketing data, socket/request/upload/download domain access, and WeChat DevTools plus real-device acceptance.

- 2026-06-03 16:31 Mini Program native media-context wiring hardening: `useLivePlaybackWiring` now forwards both `pullUrl` and `createMediaContext` into `useLivePlayerInitializer`. Before this pass, `entry.vue` supplied those values but the wiring layer dropped them, so native `live-player` / `video` initialization could update only `videoUrl` and fall back to global `uni.createVideoContext(id)` / `uni.createLivePlayerContext(id)` instead of the current portrait/landscape stage component context. This is a direct playback reliability fix for active live and replay: `pullUrl` now stays synchronized with the actual native source, Mini Window/state recovery sees the same source, and player commands resolve through `LiveBroadcastStageHost -> current stage -> createMediaContext()`.
- Verification after media-context wiring hardening: `node --check uniapp-src/src/pages/broadcast/composables/useLivePlaybackWiring.js` passes. Source checks confirm `pullUrl: ctx.pullUrl` and `createMediaContext: ctx.createMediaContext` are present in the player initializer path. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. The compressed `dist/build/mp-weixin/pages/broadcast/composables/useLivePlaybackWiring.js` contains `pullUrl:r.pullUrl` and `createMediaContext:r.createMediaContext`, and `dist/dev/mp-weixin/pages/broadcast/composables/useLivePlaybackWiring.js` contains the uncompressed source fields. Forbidden-runtime scan across broadcast, live-lottery, lottery-preview, live API, `mini-live-socket`, and `ws-envelope` reports `forbidden_hits=0`. Route coverage passes for broadcast entry/replay, legacy live vertical/horizontal, agent share-live redirect routes, center, prize record, invitation record, and report with `routeCount=196`.
- Remaining verification boundary is still open: this pass closes a concrete native playback wiring gap that can be proven from source and generated packages, but final active-live completion still requires a real `pushStatus: 1` room with Mini Program-playable pull URL plus device validation for live-player playback, realtime barrage, likes, lottery, watch reward, and product-push events.

- 2026-06-03 10:57 default smoke-room entry and generated-package verification: `useLiveEntryBootstrap` now carries the user-provided smoke link as a Mini Program debug fallback (`roomCode=wusrc6dh3wxd`, `tenantId=15`, `liveType=live`, `_tc=xthxirwe9f`, and the supplied `wx_token`). `/pages/broadcast/entry` with no explicit room target, or with `debugLive` / `useTestLive`, can enter that room directly; normal links that already provide `roomCode`, `roomId`, `liveId`, `live_id`, or `scene` are not overridden. This is only a test-entry convenience and does not change production route precedence.
- Verification after default smoke-room entry: `node --check` passes for `useLiveEntryBootstrap.js`, `useLiveEntryInitializer.js`, `useLivePlaybackWiring.js`, and `useLiveSoundIntent.js`. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin/pages/broadcast/composables/useLiveEntryBootstrap.js` at `2026-06-03 10:57:47`, and generated dev/build output contains the smoke `roomCode` / token plus the media-context wiring and room-id alias fixes. Static checks report `forbidden_hits=0` for unadapted browser globals, H5 player packages, JSSDK, dynamic import, Blob/XHR/fetch upload paths, and Agora DOM usage in the viewer live-room scope. Route coverage reports `missing=0`, `routeCount=196`; H5 broadcast runtime file comparison reports `missing=0`; H5 template static class coverage reports `missing=0`; H5 SCSS selector-class coverage reports `missing=0`. No real `uni` / `vite` / `mp-weixin` watch process remains after verification.
- Remaining verification boundary is unchanged: the smoke room `wusrc6dh3wxd` has previously returned `pushStatus: 2` / `回放` from `/h5/live/detail`, so it can exercise entry and replay fallback but cannot prove active live-player playback. Final completion still requires a currently pushing `pushStatus: 1` room with a Mini Program-playable pull URL and enabled barrage/lottery/watch-reward/product-push data, then WeChat DevTools plus real-device validation.

- 2026-06-03 11:08 Mini Program media-component selection hardening: active-live playback now keeps the player initializer's resolved media component (`live-player` vs `video`) in `mediaSourceComponent` / `mediaSourceType` and passes that state into both portrait and landscape H5-copied stages. Stage rendering now prefers this resolved component before falling back to URL sniffing. This closes a real active-live fallback risk: when a failed RTMP/FLV `live-player` source falls back to an HLS/video-compatible URL, the template switches to `<video>` instead of continuing to render `<live-player>` only because the room is live.
- The same verification pass incorporated the replay-source selection fix from the playback subtask: replay playback uses `selectReplayVideoPlaybackSource()` so Mini Program `<video>` receives playable `mp4` / `m3u8` style URLs and does not directly consume RTMP/FLV fields when a replay payload mixes live and recording aliases.
- Verification after media-component selection hardening: `node --check` passes for `utils/videoPlay.js`, `live-entry-initializer-helpers.js`, `useLiveReplayPlayback.js`, `useLivePlayerInitializer.js`, `useLivePlaybackWiring.js`, and `useLiveStageBinding.js`. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` at `2026-06-03 11:08:01`; generated dev output contains `mediaSourceComponent`, `resolveMediaSourceComponent`, and `selectReplayVideoPlaybackSource` in the broadcast/player path. Generated build output contains the compressed equivalents in `entry.js`, `useLivePlayerInitializer.js`, `live-entry-initializer-helpers.js`, `useLiveReplayPlayback.js`, and `utils/videoPlay.js`. Static forbidden-runtime scan across broadcast, `utils/videoPlay.js`, live API, and Mini socket reports `forbidden_hits=0`. Route coverage for broadcast entry/replay, legacy live vertical/horizontal, center, prize record, invitation record, and report-form reports `missing=0`, `routeCount=196`. No `dev:mp-weixin` watch process remains.
- Remaining verification boundary is still active-live data and device behavior: this fixes a source/component mismatch that can be proven statically, but final completion still requires WeChat DevTools plus real-device validation on a currently live `pushStatus: 1` room and a real replay room with reachable Mini Program-approved media URLs, socket/request/upload/download domains, and enabled barrage/lottery/watch-reward/product-push data.
- 2026-06-03 11:15 replay detail-list source normalization follow-up: the independent Trellis check pass found that detail-list replay normalization still did not reuse `selectReplayVideoPlaybackSource()`. `useLiveEntryInitializer` now applies the same replay-source selector while building `replayVideosList`, so replay detail payloads that expose only `mp4Url` / `mp4_url` are preserved, while mixed payloads no longer prefer RTMP/FLV `videoUrl` for Mini Program `<video>` playback when an mp4/m3u8-compatible alias is available.
- Verification after replay detail-list follow-up: `node --check` passes for `useLiveEntryInitializer.js`, `utils/videoPlay.js`, and `useLiveReplayPlayback.js`, plus the earlier player wiring files. `npm run build:mp-weixin` passes again with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` at `2026-06-03 11:15:16`; generated dev output contains `selectReplayVideoPlaybackSource(sourceItem)`, `mediaSourceComponent`, and `resolveMediaSourceComponent`. Generated build output contains the compressed equivalents in `useLiveEntryInitializer.js`, `useLivePlayerInitializer.js`, `entry.js`, and `utils/videoPlay.js`. Static forbidden-runtime scan still reports `forbidden_hits=0`, and no real `uni` / `vite` / `mp-weixin` watch process remains after verification.
- 2026-06-03 11:28 prize-record and lottery-result order-link hardening: Mini Program prize-record now normalizes H5-style prize payload aliases before rendering/action routing, including `record_id`, `winner_record_id`, `win_type`, `activity_type`, `reward_type`, `reward_name`, `prize_name`, `product_name`, `room_name`, `win_time`, `room_ended`, `order_id`, `order_no`, `out_trade_no`, `order_detail_url`, `order_url`, and `room_code`. The normal lottery win modal now accepts the same order/detail/record URL aliases when deciding whether "查看订单" should open order detail/list or fall back to prize record. `/h5/order/list` requests also forward `orderNo`, and the order list page accepts `orderNo/order_no/outTradeNo/out_trade_no` query params with local filtering fallback, so prize records that only carry an order number no longer terminate at a generic empty toast.
- Verification after prize-record/order-link hardening: `node --check uniapp-src/src/api/order.js` passes. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Static scan across broadcast, live-lottery, prize-record, lottery-preview, live/order APIs, and shared utils reports no actionable browser globals, H5 player packages, JSSDK, or dynamic playback regressions; the remaining hits are existing Mini Program-safe `windowInfo` naming and `slot-machine-window` class names.

- 2026-06-03 11:51 Mini Program WebSocket barrage payload identity hardening: `MiniLiveSocket.sendChat()` now mirrors the H5 IM direct-send payload shape more closely by emitting `roomId/room_id`, `liveId/live_id`, `msgId/msg_id`, `clientMsgId/client_msg_id`, and audience aliases (`userId/user_id`, `customerId/customer_id`, `nickname/nick/userName/customerName`, avatar/head image aliases) inside the chat `data` payload while also keeping message id aliases at the top level. Caller-provided replay metadata such as `timelineSeconds` and `replay_video_id` is preserved, so replay barrage, comment lottery, and backend comment persistence do not lose identity or timeline context when the Mini Program socket fallback path is used.
- Verification after barrage payload hardening: `node --check uniapp-src/src/utils/mini-live-socket.js` passes. A focused `sendChat()` behavior check with fake `uni` and socket send confirms the emitted body contains top-level `roomId/room_id`, top-level `msgId/msg_id/clientMsgId/client_msg_id`, nested `roomId/room_id/liveId/live_id`, nested user/customer/nickname/avatar aliases, and preserved replay metadata. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited cleanly, with no remaining `dev:mp-weixin` watch process. Generated build/dev output both contain the updated chat alias fields. Route coverage passes with main package and subPackages expanded (`routeCount: 196`, `missing: []`, `missingVue: []`) for broadcast entry/replay, legacy live vertical/horizontal, center, order list/detail, prize record, invitation record, and report form. A refined forbidden-runtime scan across 157 viewer live-room/API/socket files reports `hits: []` for unadapted browser globals, H5 player package imports, JSSDK imports, dynamic browser upload paths, and the earlier `videoPlayer` / `handlePageHide` runtime-error strings.
- Remaining verification boundary is still active-live data and real-device behavior: these checks prove the source and generated Mini Program packages contain the barrage payload fix and have no static H5 runtime regressions, but they do not prove active `live-player` media playback, real-time socket barrage delivery, likes, lottery, watch reward, or live product-push events against production infrastructure. Final completion still requires a currently live `pushStatus: 1` room with a Mini Program-playable RTMP/FLV/HLS pull URL, enabled non-payment live interactions, valid auth, configured request/socket/upload/download/live-player domains, and WeChat DevTools plus real-device verification.

- 2026-06-03 11:58 H5-compatible interaction request payload hardening: live-room non-payment interaction APIs now emit camelCase and snake_case aliases for H5 backend compatibility. `api/live.js` wraps live status/products/current product/comment history/like/buy reminder/sign/enter/leave/heartbeat/progress payloads with aliases such as `roomId/room_id/liveId/live_id`, `termId/term_id/liveTermId/live_term_id`, `watchDuration/watch_duration`, `watchStatus/watch_status`, `lastPosition/last_position`, `videoId/video_id/replayVideoId/replay_video_id`, `productId/product_id/goodsId/goods_id`, `pageSize/page_size`, and `formData/form_data`. `api/marketing.js` now builds H5-compatible payloads for prize records, lottery participants, comment lottery list/detail, `claimWatchReward`, and `claimCommentReward`, including `activityId/activity_id`, `prizeId/prize_id`, `roomId/room_id/liveId/live_id`, `termId/term_id`, `customerId/customer_id/userId/user_id`, `roomCode/room_code`, and `comment/content`.
- The calling composables now pass the missing runtime context into those APIs: `useLiveWatchRewards` includes live room id, term id, customer/user id, and room code when claiming a watch reward; `useLiveCommentLottery` includes the same room/term/customer aliases plus `activity_id`, `prize_id`, and `content` when using the fallback comment-lottery participation endpoint. This keeps the copied H5 UI/interaction flow but makes the Mini Program payload acceptable to both camelCase and snake_case H5 backend parsers.
- Verification after interaction payload hardening: `node --check` passes for `api/live.js`, `api/marketing.js`, `useLiveWatchRewards.js`, `useLiveCommentLottery.js`, and `useLiveMarketingRuntime.js`. A source alias coverage check confirms all expected request aliases are present. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited cleanly, with no remaining `dev:mp-weixin` watch process. Generated build/dev output contains the updated aliases in `api/live.js` and `api/marketing.js`. Route coverage passes with main package and subPackages expanded (`routeCount: 196`, `missing: []`, `missingVue: []`) for broadcast entry/replay, legacy live vertical/horizontal, center, order list/detail, prize record, invitation record, and report form. The refined forbidden-runtime scan across 157 viewer live-room/API/socket files reports `hits: []`.
- Remaining verification boundary is unchanged: the code and generated packages now carry stronger request payload compatibility for likes, sign-in, watch reward, comment lottery, product queries, buy reminders, and replay progress, but final completion still requires a currently live `pushStatus: 1` room plus real WeChat DevTools/device validation for active playback, realtime barrage delivery, likes, lottery, watch reward, product-push events, report/share, and replay/live switching.

- 2026-06-03 12:06 complaint/report payload compatibility hardening: the Mini Program complaint API now wraps upload/create/list/detail requests with H5-compatible aliases for `roomId/room_id/liveId/live_id`, `roomCode/room_code`, `complaintType/complaint_type/type`, `complaintId/complaint_id/id`, `reporterPhone/reporter_phone/phone/mobile`, `images/imageUrls/image_urls/evidenceImages/evidence_images`, `filename/fileName/file_name`, and `contentType/content_type/mimeType/mime_type`. The live-room report popup now receives `roomCode` from both portrait and landscape stages and passes both room/live aliases into image upload plus complaint creation. The standalone report home/type/form flow also preserves `roomCode`, falls back to `live_room_context`, and submits the same alias set; a visible typo was corrected from `举报说明1` to `举报说明`.
- Verification after complaint/report hardening: `node --check uniapp-src/src/api/complaint.js` passes. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and no `dev:mp-weixin` watch process remains. Route coverage with main package and subPackages expanded still reports `routeCount: 196`, `missing: []`, `missingVue: []` for broadcast entry/replay, legacy live vertical/horizontal, center, order list/detail, prize record, invitation record, and report form. A refined forbidden-runtime scan across 157 viewer live-room/API/socket files reports `hits: []`. Generated build and dev outputs contain the complaint alias fields in `api/complaint.js`, `components/live-report-popup.js`, and `pages/report/report-form.js`.
- Remaining verification boundary is still active-live/device-side: this pass improves report/share-adjacent complaint submission compatibility and can be proven from source/generated packages, but final completion still requires a currently live `pushStatus: 1` room with Mini Program-playable media and real device validation for live-player playback, realtime barrage, likes, lottery, watch reward, product-push events, report/share, and replay/live switching.

- 2026-06-03 12:20 share attribution and generated-package verification: Mini Program share/invitation flow now preserves H5 live-room attribution context through native share paths. `share-popup` carries `shareCode` from `/h5/live/distributorShareUrl`, emits it back to the live entry action handler, builds Mini Program paths with `shareCode` plus `bindId`, and stores both values in the invitation payload. `entry.vue`, `useLiveEntryInitializer`, `useLiveEntryActions`, `useLiveStageBinding`, portrait/landscape stages, `live-route`, and `/pages/invitation/index` now pass the same context into `onShareAppMessage`, invitation posters, route normalization, persisted live room context, and `/h5/live/enterLiveRoom` reporting. This keeps H5 distributor/share attribution semantics while using Mini Program-native share and page navigation.
- Verification after share attribution hardening: `node --check` passes for the touched route/action/initializer/stage-binding files. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited by timeout with no remaining `dev:mp-weixin`, `uni -p mp-weixin`, or workspace watch process. Route coverage with main package and subPackages expanded reports `routeCount: 196`, `missing: []`, `missingVue: []` for broadcast entry/replay, legacy live vertical/horizontal, center, order list/detail, prize record, invitation record, report form, and invitation page. A refined forbidden-runtime scan across 160 viewer live-room/API/socket/share/invitation files reports `hits: []`, including no `videoPlayer is not defined` or `handlePageHide is not defined` strings. Generated build and dev outputs both contain the updated `shareCode`, `bindId`, and `enterLiveRoom` paths.
- Remaining verification boundary is unchanged: this proves share attribution context and generated packages are updated locally, but final goal completion still requires a real active `pushStatus: 1` room with Mini Program-playable media, valid auth, socket/request/upload/download/live-player domain access, and enabled barrage/lottery/watch-reward/product-push data for WeChat DevTools plus real-device acceptance.

- 2026-06-03 12:26 Mini Program socket outbound room-alias hardening: `MiniLiveSocket` now builds a shared room payload and attaches `roomId`, `room_id`, `liveId`, and `live_id` at both top level and nested `data` level for chat, like, enter, leave, and replay-request messages. Chat also keeps top-level audience aliases plus nested audience aliases and `msgId`/`msg_id`/`clientMsgId`/`client_msg_id`. This closes a mismatch between previous documentation and actual source where chat only nested room aliases, which could let H5/IM backend routing, signing, or fan-out paths miss Mini Program messages even when the local optimistic comment rendered.
- Verification after socket outbound hardening: `node --check uniapp-src/src/utils/mini-live-socket.js` passes. A focused fake-socket Node behavior test confirms emitted chat/like/enter/leave/replay request payloads all contain top-level and nested `roomId/room_id/liveId/live_id`, chat identity aliases, client message id aliases, and replay `sinceSeq`. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Route coverage reports `routeCount: 196`, `missing: []`, `missingVue: []`. A refined forbidden-runtime scan across 160 viewer live-room/API/socket/share/invitation files reports `hits: []`. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and no `dev:mp-weixin` / `uni -p mp-weixin` process remains. Source, build, and dev outputs contain `buildRoomPayload`, `client_msg_id`, and replay `sinceSeq`.
- Remaining verification boundary is still active-live/device-side: this pass proves outbound socket payload shape locally, but final completion still requires a currently live `pushStatus: 1` room with Mini Program-playable media, configured socket/request/upload/download/live-player domains, and real WeChat DevTools plus device validation for remote barrage delivery/display, likes, lottery, watch reward, product events, report/share, and replay/live switching.

- 2026-06-03 12:35 HTTP comment fallback payload hardening: the WebSocket send path already carries H5-compatible room/user aliases, but the `/h5/live/comment` HTTP fallback only had live id, text, and message ids. `useLiveComments` now passes `roomCode/room_code`, effective `termId/term_id`, `customerId/customer_id/userId/user_id`, nickname/name aliases, avatar/head image aliases, and replay timeline/video aliases into both WebSocket and HTTP fallback sends. `api/live.js` now expands the same aliases at the top level and nested `data` level for `/h5/live/comment`, so backend paths that parse H5 form fields or nested IM-style data receive the same room/session/user context.
- Verification after HTTP comment fallback hardening: `node --check` passes for `api/live.js` and `useLiveComments.js`. A focused mocked `sendLiveComment()` payload test confirms `/h5/live/comment` receives top-level and nested `roomId/room_id/liveId/live_id`, `roomCode/room_code`, `termId/term_id`, `customerId/customer_id/userId/user_id`, `msgId/msg_id/clientMsgId/client_msg_id`, `timelineSeconds/timeline_seconds`, and `replayVideoId/replay_video_id/videoId/video_id`. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Route coverage reports `routeCount: 196`, `missing: []`, `missingVue: []`. A refined forbidden-runtime scan across 160 viewer live-room/API/socket/share/invitation files reports `hits: []`. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`, no watch process remains, and build/dev outputs contain the new HTTP comment context fields.
- Remaining verification boundary is unchanged: this closes a local HTTP fallback payload gap for barrage/comment delivery, but final completion still needs a currently active live room and real WeChat DevTools/device validation to prove remote comment fan-out, active playback, likes, lottery, watch reward, product pushes, report/share, and replay/live switching.

- 2026-06-03 12:47 marketing lottery context hardening: comment-lottery and ordinary-lottery request paths now carry the same H5-compatible room context as barrage/watch-reward paths. `useLiveCommentLottery` builds a shared context with `roomId/room_id/liveId/live_id`, `termId/term_id/liveTermId/live_term_id`, `customerId/customer_id/userId/user_id`, and `roomCode/room_code`, and sends it through comment-lottery list, detail refresh, participant lookup, and `claimCommentReward`. Comment-lottery claim also emits `activityType/activity_type` and `winType/win_type`. `useLiveNormalLottery` now receives `roomCode/liveId/myUserId/getEffectiveTermId` from `useLiveMarketingRuntime` and includes those aliases when loading ordinary-lottery participants. `api/marketing.js` now normalizes activity/prize/room/term/customer/activity-type aliases and mirrors claim payload aliases into nested `data` for `claimWatchReward` and `claimCommentReward`, while keeping GET query payloads flat.
- Verification after marketing lottery context hardening: `node --check` passes for `api/marketing.js`, `useLiveNormalLottery.js`, `useLiveCommentLottery.js`, and `useLiveMarketingRuntime.js`. Focused Node behavior checks confirm ordinary-lottery participant lookup and comment-lottery list/detail/participant/claim calls all include room, term, customer, roomCode, activity, prize, draw, and comment aliases as expected; a separate marketing API payload check confirms claim POSTs include both top-level and nested `data` aliases while comment-lottery GET stays flat. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Route coverage with main package and subPackages expanded passes for 11 viewer-loop routes including broadcast entry/replay, legacy live vertical/horizontal, center, order list/detail/confirm, address, prize record, invitation record, and report. A refined forbidden-runtime scan across broadcast and lottery-preview reports no actionable browser globals or H5 player/JSSDK imports; the only literal `window` hit is the CSS class name `slot-machine-window`. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated dev output contains `buildRoomContext`, `room_code`, `activity_type`, `claimWatchReward`, and `claimCommentReward` in the marketing/runtime paths. No `dev:mp-weixin` / `uni -p mp-weixin` watch process remains after cleanup.
- Remaining verification boundary is unchanged: this closes local payload/context gaps for normal lottery, comment lottery, watch reward claims, and marketing participant/list/detail calls, but final goal completion still requires a currently active `pushStatus: 1` live room with Mini Program-playable media, valid auth, socket/request/upload/download/live-player domain allowlists, and real WeChat DevTools plus device validation for live-player playback, realtime barrage fan-out, likes, ordinary lottery, comment lottery, watch reward, product pushes, report/share, and replay/live switching.

- 2026-06-03 12:56 sign-in room/user context hardening: the live sign-in path now carries H5-compatible room, term, and audience identity aliases from the live-room runtime into both sign status checks and sign submit requests. `api/live.js` now normalizes `roomCode/room_code`, `roomId/room_id/liveId/live_id`, `termId/term_id/liveTermId/live_term_id`, `customerId/customer_id`, and `userId/user_id`; `submitSign` mirrors the same aliases at the top level plus nested `data`, while keeping the H5 `formData/form_data` shape. `live-sign-in.vue`, `useLiveSidePanels`, `useLiveStageBinding`, `entry.vue`, and the portrait/landscape H5-copied stages now pass `roomCode`, current term id, and current user/customer id into the sign-in component without changing the copied visual structure.
- Verification after sign-in context hardening: `node --check` passes for `api/live.js`, `services/live-sign.js`, `useLiveSidePanels.js`, and `useLiveStageBinding.js`; focused mocked payload checks confirm `/h5/live/sign/check` and `/h5/live/sign/submit` receive top-level room/term/customer/user aliases and nested submit `data` aliases. `npm run build:mp-weixin` passes with only Sass/Wot Design deprecation warnings. Route coverage passes for broadcast entry/replay, legacy live vertical/horizontal, center, prize record, report, and related viewer-loop routes. A focused forbidden-runtime scan across broadcast/sign/API scope reports no actionable browser globals or H5 player/JSSDK imports. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`, generated output contains `room_code`, `form_data`, `checkSigned`, `submitSign`, `roomCurrentTermId`, and `myUserId` in the expected API/sign/stage files, and no `dev:mp-weixin` / `uni -p mp-weixin` watch process remains.
- Remaining verification boundary is unchanged: this closes a local payload/context gap for sign-in, but final goal completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, and real WeChat DevTools plus device validation for live-player playback, remote barrage delivery/display, likes, ordinary lottery, comment lottery, watch reward, product pushes, report/share, sign-in, and replay/live switching.

- 2026-06-03 13:33 product shelf/current-product/buy-reminder context hardening: the live-room product chain now carries H5-compatible product and room context across API, WS, product detail navigation, and order-confirm entry. `api/live.js` accepts object payloads for `getLiveProducts` and `getCurrentProduct`, sends `page/current/pageSize/page_size/limit`, and mirrors `roomId/room_id/liveId/live_id`, `roomCode/room_code`, `termId/term_id/liveTermId/live_term_id`, `productId/product_id/goodsId/goods_id`, and SKU aliases in `/h5/live/buyReminder`. `api/product.js` now wraps `/h5/live/productDetail` with the same product/room/term/SKU/tenant/share aliases. `useLiveProducts` maps H5 product fields such as `product_id`, `product_name`, `product_price`, `product_stock`, `stock_num`, `spec_type`, `product_sku_id`, `schedule_*`, and `video_id` into the existing copied H5 UI fields while preserving the original payload. `useLiveWsMessageHandler` now normalizes product pushes where the product is nested under `product/currentProduct/current_product/goods/goodsInfo/item` without losing top-level `action/status`, so current explained product, product list, stock, and status events survive H5/IM envelope shape drift.
- Product entry continuity was also hardened: `useLiveEntryActions` now navigates to `/pages/product/detail/detail` with both camelCase and snake_case product, room, term, SKU, tenant, roomCode, and shareCode query fields. `/pages/product/detail/detail` reads both naming styles and passes roomCode, termId, tenantId, and shareCode into its H5-style spec popup. `pages/product/detail/popup/spec.vue` now forwards that context to `/pages/order/confirm`, and `pages/order/confirm.vue` now parses direct query params as well as `payload`, so live product detail and order confirmation no longer depend on a payload-only path to keep live room attribution.
- Verification after product-chain hardening: `node --check` passes for `api/live.js`, `api/product.js`, `useLiveProducts.js`, `useLiveEntryActions.js`, `useLivePurchase.js`, and `useLiveWsMessageHandler.js`. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. Normalized route coverage passes for broadcast entry/replay, legacy live vertical/horizontal, product detail, and order confirm (`routeCount: 196`, no missing routes or `.vue` files). A focused forbidden-runtime scan across broadcast, product-detail, order-confirm, API, socket, and service scopes reports no actionable browser globals or H5 player/JSSDK imports. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated dev output contains `room_code`, `product_sku_id`, `live_term_id`, `buyReminder`, `getProductEventPayload`, `buildLiveQuery`, and `applyOrderQuery` in the expected product-chain files, and no `dev:mp-weixin` / `uni -p mp-weixin` watch process remains.
- Remaining verification boundary is unchanged: this closes local and generated-package gaps for product shelf entry, current explained product, product list payloads, buy reminders, product detail navigation, and order-confirm live context. It does not prove active `live-player` playback or production WS fan-out. Final goal completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, and real WeChat DevTools plus device validation for playback, barrage, likes, ordinary lottery, comment lottery, watch reward, product pushes, product purchase entry, report/share, sign-in, and replay/live switching.

- 2026-06-03 14:02 debug live entry credential cleanup and playback-source audit: `useLiveEntryBootstrap` no longer embeds the smoke room or JWT in source. The default debug entry is empty, normal `/pages/broadcast/entry` links are never overridden, and the debug-room fallback is only attempted when `useTestLive` / `testLive` / `debugLive` / `debug_live` is explicitly provided and local Mini Program storage has `debug_live_room_code` / `debug_live_wx_token` values. This removes the regression where no-param entry or generated `dist/dev/mp-weixin` could silently enter the replay-only smoke room and mask real active-live validation.
- Playback source checks after the cleanup confirm the existing native Mini Program media path still matches the H5 migration contract: `getMiniProgramLiveCandidates(detail, streamInfo)` merges detail plus `/h5/live/streamInf`, sorts RTMP before FLV before HLS, maps RTMP/FLV to `live-player`, maps HLS/replay to `video`, and route normalization still handles `scene=live_id:123`, `live_id=123`, `roomCode=xxx`, and `replay=1`.
- Verification after debug cleanup: `node --check uniapp-src/src/pages/broadcast/composables/useLiveEntryBootstrap.js` passes. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; source, `dist/build/mp-weixin`, and `dist/dev/mp-weixin` scans find no `wusrc6dh3wxd`, old token fragments, `eyJhbGci`, `HARDCODED_TEST_LIVE_ENTRY`, or `applyHardcodedTestLiveEntry`. Generated build/dev `useLiveEntryBootstrap.js` both show empty debug defaults. Normalized route coverage passes for broadcast entry/replay, legacy live vertical/horizontal, product detail, order confirm/list/detail, address, prize record, invitation record, and report (`routeCount: 196`, no missing routes or `.vue` files). The focused forbidden-runtime scan across broadcast, lottery-preview, live-lottery, live API, `videoPlay`, `live-route`, platform, and services reports only known non-runtime false positives: the `slot-machine-window` CSS class and the platform README text that explicitly forbids browser upload APIs. No current-workspace `dev:mp-weixin` / `uni -p mp-weixin` watch process remains after cleanup.
- Remaining verification boundary is unchanged and still blocks marking the goal complete: these checks prove the generated Mini Program packages no longer carry test credentials and that the local media-source routing remains correct, but they do not prove active `live-player` playback or production interaction fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable RTMP/FLV/HLS, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, ordinary lottery, comment lottery, watch reward, product pushes, report/share, sign-in, and replay/live switching.

- 2026-06-03 13:44 watch-reward WebSocket envelope hardening without subagents: `useLiveWatchRewards` now uses the same payload-unwrapping boundary pattern as the lottery runtime, accepting top-level message fields, `payload`, `data`, and `data.payload` while preserving H5 camelCase plus Mini Program snake_case aliases. Watch-reward event detection now recognizes `activityType/activity_type/winType/win_type === 1` as before, plus explicit watch-reward markers such as `watchRewardActivityId/watch_reward_activity_id`, `watchRewardId/watch_reward_id`, and watch-duration/watch-reward event names. Lifecycle reload, self win-notify result display, and room-wide watch-reward broadcast all flow through this normalized payload so H5 backend envelope drift does not silently drop the UI update. `mini-live-socket` also recognizes additional watch-reward event aliases (`watch_reward_win`, `watch_reward_winner`, `watch_reward_result`, `watch_duration_reward_*`, activity update/open/close/delete, and broadcast/notice variants) and maps them into the existing Mini Program handlers.
- Verification after watch-reward WS hardening: `node --check` passes for `uniapp-src/src/pages/broadcast/composables/useLiveWatchRewards.js` and `uniapp-src/src/utils/mini-live-socket.js`. A focused forbidden-runtime scan of those changed files reports no browser globals, H5 player packages, JSSDK, DOM insertion, `fetch`, `Blob`, `XMLHttpRequest`, `videoPlayer is not defined`, or `handlePageHide is not defined` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. This pass intentionally avoided spawning or waiting for subagents after the agent thread-limit issue; all checks were run in the main thread.
- Remaining verification boundary is unchanged and still blocks declaring the full live-room migration complete: this proves the local source/build path for watch-reward WS normalization, but not production event fan-out or active media playback. Final acceptance still needs a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, and replay/live switching.

- 2026-06-03 13:52 active-live status payload envelope hardening: `live-status-snapshot` now unwraps H5/IM status envelopes before applying playback state. It recognizes live status fields in `data.payload`, `payload`, `data`, or top-level message objects, and treats bare `status` as a valid status marker alongside `pushStatus/push_status/liveStatus/live_status`. This protects the active-live recovery/switch path where `/h5/live/status` or `live_status_update` events can carry the new pull URL under a nested payload; previously such messages could leave `pushStatus`/`pullUrl` unresolved and fail to call `initVideoPlayer`, so an active room or stream switch might not restore Mini Program playback. The change is limited to the status snapshot boundary and does not alter copied H5 stage layout or global socket routing.
- Verification after active-live status payload hardening: `node --check` passes for `uniapp-src/src/pages/broadcast/utils/live-status-snapshot.js` and `uniapp-src/src/pages/broadcast/composables/useLiveWsMessageHandler.js`. A focused forbidden-runtime scan of `live-status-snapshot.js` reports no browser globals, H5 player packages, JSSDK, DOM insertion, `fetch`, `Blob`, `XMLHttpRequest`, `videoPlayer is not defined`, or `handlePageHide is not defined` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated dev/build output both contain the `data.data.payload` unwrapping and `status` marker support, and no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains.
- Remaining verification boundary is unchanged: this improves the source/generated active-live status-to-player path, but does not prove production `live-player` playback. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, and replay/live switching.

- 2026-06-03 13:57 active-live playback source envelope hardening without subagents: after the agent thread-limit issue, this pass stayed on the main thread and did not spawn or wait for any subagents. `utils/live-route.js` now recursively unwraps H5/common backend media source containers such as `payload`, `data`, `data.payload`, `streamInfo/stream_info`, `liveStream/live_stream`, `pullStream/pull_stream`, `room`, and `detail` before classifying Mini Program media candidates. `pages/broadcast/utils/live-source.js` now applies the same deep source collection before normalizing quality streams, so payloads like `{ data: { payload: { pull_streams: [...] } } }` and `{ data: { streamInfo: { live_streams: [...] } } }` no longer lose their FLV/RTMP/HLS playback URLs.
- Verification after playback-source envelope hardening: `node --check` passes for `uniapp-src/src/utils/live-route.js` and `uniapp-src/src/pages/broadcast/utils/live-source.js`. A focused forbidden-runtime scan of those files reports no browser globals, H5 player packages, JSSDK, DOM insertion, `fetch`, `Blob`, `XMLHttpRequest`, `videoPlayer is not defined`, or `handlePageHide is not defined` hits. A focused source behavior test confirms nested `data.payload.pull_streams` emits a FLV `live-player` candidate and an HLS `video` candidate. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated dev/build output both contain the new deep source object fields including `stream_info` and `live_streams`, and no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains.
- Remaining verification boundary is unchanged: this closes another local/generated active-live source parsing gap, but it still does not prove production `live-player` playback or remote interaction fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, and replay/live switching.

- 2026-06-03 14:21 broadcast WebSocket business-dispatch type normalization without subagents: Trellis implement dispatch was attempted once but failed with `agent thread limit reached`, so this pass stayed in the main thread and did not retry subagent orchestration. `useLiveWsMessageHandler` now normalizes incoming H5/IM/WS business messages at the broadcast dispatch boundary before the existing copied H5 switch handlers run. The new `normalizeBroadcastWsMessage()` accepts numeric H5 message types plus `event/eventType/event_type/msgType/msg_type/messageType/message_type/customEvent/custom_event/eventName/event_name/name/cmd/action/operation/op` from top-level, `data`, `payload`, and `data.payload`, and maps them to the existing business types for chat, likes, enter/leave, product events, settings, live status, comment lottery, ordinary lottery, watch reward lifecycle/broadcast, buy reminders, and moderation events. This prevents non-standard but H5-compatible envelopes from being dropped by the previous `if (!data || !data.type) return` guard before barrage, likes, lottery, reward, product, or status handlers could run.
- Verification after WS dispatch normalization: `node --check uniapp-src/src/pages/broadcast/composables/useLiveWsMessageHandler.js` passes. A focused extracted normalization test confirms `{ type: 1 } -> chat`, `customEvent=watch_duration_reward_broadcast -> watch_reward_broadcast`, `data.payload.event=beginCommentLotteryPrize -> comment_lottery_event`, `data.message_type=liveStatusSnapshot -> live_status_update`, `msg_type=productStock -> product_stock`, and `eventName=buyReminder -> r_to_buy`. A focused forbidden-runtime scan of the changed file reports no browser globals, H5 player packages, JSSDK, DOM insertion, `fetch`, `Blob`, `XMLHttpRequest`, `videoPlayer is not defined`, or `handlePageHide is not defined` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated dev/build output both contain the new numeric map and alias normalization (`WS_NUMERIC_TYPE_MAP`, `watch_duration_reward_broadcast`, `begin_comment_lottery_prize`, `message_type`), and no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains.
- Remaining verification boundary is unchanged: this improves the generated package's ability to route production interaction events into the migrated H5 business handlers, but it still does not prove active `live-player` playback or remote event fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, and replay/live switching.

- 2026-06-03 14:13 replay share/invitation route-state preservation without subagents: after the user explicitly called out the repeated agent-shutdown stall, this pass stayed entirely on the main thread and did not attempt to spawn, close, or wait for subagents. The broadcast share path, share popup, and invitation page now preserve replay context across native Mini Program sharing, generated invitation cards, QR code links, copied links, and H5-hash-style share URLs. `entry.vue` appends `mode=replay`, `replay=1`, `liveType=replay`, and `videoId/video_id/replayVideoId/replay_video_id` when sharing from replay. `share-popup.vue` receives `isReplay` plus `replayCurrentVideoId` from both portrait and landscape H5-copied stages, appends replay params to Mini Program paths, stores them in `invitation_payload`, and patches distributor/long-link URLs without losing `#/pages/broadcast/entry?...` hash routing. `pages/invitation/index.vue` now accepts the same replay aliases from storage and rebuilds share card paths without dropping replay/video state.
- Verification after replay share/invitation preservation: `node --check uniapp-src/src/pages/broadcast/composables/useLiveStageBinding.js` passes. A focused forbidden-runtime scan across the changed broadcast/share/invitation files reports no browser globals, H5 player packages, JSSDK, DOM insertion, `fetch`, `Blob`, `XMLHttpRequest`, `videoPlayer is not defined`, or `handlePageHide is not defined` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and stopped via timeout with no remaining watch session; generated dev output contains `mode=replay`, `liveType=replay`, `replayVideoId`, `replay_video_id`, `withReplayParams`, and `isReplayPayload` in the expected broadcast/share/invitation files.
- Remaining verification boundary is unchanged: this closes a generated-package gap for replay share/return continuity, but it still does not prove active `live-player` playback or production interaction fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, replay sharing, and replay/live switching.

- 2026-06-03 14:20 report/complaint return-to-live route context preservation without subagents: `trellis-implement` dispatch was attempted once at session start as required by workflow but failed with `agent thread limit reached`; this pass then stayed fully in the main thread and did not retry, wait for, or close subagents. A new `pages/broadcast/utils/live-route-context.js` helper builds canonical broadcast return paths with `roomCode`, `liveId`, tenant/share/bind context, and replay aliases (`mode=replay`, `replay=1`, `liveType=replay`, `videoId/video_id/replayVideoId/replay_video_id`). `entry.vue` now computes `broadcastReturnPath` from current live/replay state and passes it into both portrait and landscape H5-copied stages; `live-report-popup` receives that dynamic `fromPath` instead of the previous hard-coded `/pages/broadcast/entry`. The live-room context cache saved by `useLiveEntryInitializer` also preserves replay flags and video id aliases so report pages opened from center/secondary routes can still reconstruct the correct replay/live return target.
- The standalone H5-style report pages now preserve the same return context across the full complaint flow: `report-home`, `report-type`, and `report-form` load `live_room_context` when `liveId`, `roomCode`, or `fromPath` is missing, forward `fromPath` while switching report type, and redirect the success page with the full return path. This fixes the failure mode where a complaint submitted from a replay room or a specific shared live room could return to a generic `/pages/broadcast/entry` and lose the room/replay/video state.
- Verification after report return-context preservation: `node --check` passes for `pages/broadcast/utils/live-route-context.js`, `useLiveStageBinding.js`, and `useLiveEntryInitializer.js`. A focused forbidden-runtime scan across the changed broadcast/report files reports no browser globals, H5 player packages, JSSDK, DOM insertion, `fetch`, `Blob`, `XMLHttpRequest`, `videoPlayer is not defined`, or `handlePageHide is not defined` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and stopped via timeout with no remaining watch session; generated dev output contains `buildBroadcastReturnPath`, `broadcastReturnPath`, dynamic `from-path`, `!liveId || !roomCode || !fromPath` cache fallback, and replay return aliases in the expected broadcast/report files.
- Remaining verification boundary is unchanged: this closes a local/generated package gap for complaint/report return-to-live continuity, but final goal completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 14:31 barrage/comment payload compatibility hardening without subagents: after the user called out the repeated agent-shutdown stall, this pass stayed fully on the main thread and did not attempt to spawn, close, or wait for subagents. The live comment chain now tolerates H5 backend field drift across send, history, and WebSocket display paths. `/h5/live/commentHistory` requests include H5-compatible `roomCode/room_code`, `termId/term_id`, `customerId/customer_id`, `userId/user_id`, `page/current`, `pageSize/page_size/limit`, and replay video aliases. `/h5/live/comment` HTTP fallback and Mini Program socket `sendChat` now mirror comment text as `content/comment/message/text` at both top level and nested `data`, while preserving `msgId/clientMsgId`, room, replay, term, and audience aliases. Incoming WS and history comments now render text from `content`, `comment`, `message`, or `text`, so H5-compatible envelopes no longer produce empty barrage rows or fail optimistic-message matching when the server uses a non-`content` field name.
- Verification after barrage/comment compatibility hardening: `node --check` passes for `uniapp-src/src/api/live.js`, `uniapp-src/src/pages/broadcast/composables/useLiveComments.js`, `uniapp-src/src/pages/broadcast/composables/useLiveWsMessageHandler.js`, and `uniapp-src/src/utils/mini-live-socket.js`. A focused forbidden-runtime scan of those files reports no browser globals, H5 player packages, JSSDK, or Agora DOM insertion hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and stopped via timeout; generated dev output contains `getCommentContent`, `getPayloadContent`, `payload.comment`, `payload.message`, `payload.text`, `pageSize`, and `page_size` in the expected API/socket/comment files. No current-workspace `dev:mp-weixin` / `uni -p mp-weixin` watch process remains.
- Remaining verification boundary is unchanged: this improves generated-package barrage/comment robustness, including HTTP fallback and replay history display, but it still does not prove production socket fan-out or active `live-player` playback. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 14:42 active-live media source classification follow-up without subagents: after the user pointed out the repeated subagent shutdown stall, this pass did not spawn, close, or wait for subagents. Work stayed on the main thread. `utils/live-route.js` now uses one shared classifier for `getMiniProgramLiveCandidates()`, `isLivePlayerSource()`, and `isVideoSource()`, with broader Mini Program media hints for RTMP, FLV, HLS, and MP4. Query hints such as `format/type/protocol/play_type/stream_type/ext/hdl=flv|hls|m3u8|mp4`, encoded URLs, `.m3u8/`, `/hls/`, `.mp4/`, and `.flv/` are recognized. This reduces the concrete active-live failure mode where an H5 backend generic `pullUrl/playUrl/url` carrying a video-compatible HLS/MP4 stream could be misclassified as `live-player`, while unknown HTTP live URLs still conservatively remain `live-player`.
- The requested test entry is wired into `useLiveEntryBootstrap` debug defaults: `testLive=1`, `debugLive=1`, or `debug_live=1` can fall back to room `wusrc6dh3wxd`, tenant `15`, `_tc=xthxirwe9f`, and the provided JWT unless storage overrides are set. This is for local verification only and should be removed or reset before a production commit if credentials must not be shipped.
- Verification after media-source classification follow-up: `node --check` passes for `uniapp-src/src/utils/live-route.js` and `uniapp-src/src/pages/broadcast/composables/useLiveEntryBootstrap.js`. A focused ESM behavior test confirms RTMP/FLV select `live-player`, HLS/MP4 select `video`, and unknown live HTTP URLs remain `live-player`. A focused forbidden-runtime scan across broadcast and the changed route/bootstrap files reports no browser globals, H5 player packages, JSSDK, or Agora DOM insertion hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/dev/mp-weixin` and `dist/build/mp-weixin` both contain the new media-source classifier, and `dist/dev` contains the requested debug room defaults.
- Remaining verification boundary is unchanged: this closes a local/generated playback-source routing gap but still does not prove production `live-player` playback. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 14:48 Mini Program page-show playback resume hardening without subagents: Trellis implement/check dispatches were each attempted once and both failed with `agent thread limit reached`; this pass then stayed fully in the main thread and did not retry, wait for, or close subagents. `useLiveVideoRuntime` now accepts `videoFrameReady` and `mediaSourceComponent`, and `resumeVideoPlayback()` can be called with `{ force: true }`. On page show, `useLiveEntryLifecycle` now forces one playback recovery instead of trusting possibly stale `isPlaying` state after Mini Program background/foreground transitions. The runtime now attempts both the existing wrapped player and the currently rendered native Mini Program media context, preferring `video` for replay/video sources and `live-player` for active live sources, and calls `play` plus `resume` for `live-player` when available. `entry.vue` wires the current media component state into this runtime.
- This closes a local playback-resume failure mode: after hide/show or native context recreation, the old wrapper could still exist and `isPlaying` could remain true while the real `live-player`/`video` had stopped. Previously the resume path could return early or stop after calling the stale wrapper. The new path replays the current native context without touching copied H5 class names or stage layout.
- Verification after page-show playback resume hardening: `node --check` passes for `uniapp-src/src/pages/broadcast/composables/useLiveVideoRuntime.js` and `uniapp-src/src/pages/broadcast/composables/useLiveEntryLifecycle.js`. A focused forbidden-runtime scan across `useLiveVideoRuntime.js`, `useLiveEntryLifecycle.js`, and `entry.vue` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `videoPlayer is not defined`, or `handlePageHide is not defined` hits. A focused ESM behavior test confirms forced resume calls the wrapped player and the `live-player` native context for active live, and prefers the `video` native context for replay. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/build/mp-weixin/pages/broadcast/composables/useLiveVideoRuntime.js` contains the new forced resume/native context logic.
- Current local state note: despite the earlier 14:02 cleanup entry, `useLiveEntryBootstrap` currently contains the user-provided test room/JWT debug defaults again for local smoke testing (`testLive=1` / `debugLive=1`). This must be removed or moved to local-only configuration before any production commit if credentials must not ship.
- Remaining verification boundary is unchanged: this improves generated-package resilience for page-show playback restoration, but it still does not prove active production `live-player` playback or remote interaction fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push data, and WeChat DevTools plus real-device validation for playback, background/foreground resume, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 14:55 sign-in payload/status compatibility hardening without subagents: after the user explicitly called out the repeated agent-shutdown stall, this pass stayed fully on the main thread and did not attempt to spawn, close, or wait for subagents. `/h5/live/sign/submit` now keeps the H5-style `{ roomId, formData }` contract while also sending top-level form fields, `form_data`, and nested `data` aliases (`formData/form_data`, `form/signForm/sign_form`) so Mini Program sign-in submissions can be accepted by H5 backend parsers that read either direct form fields or nested payloads. The sign status parser now unwraps top-level, `payload`, `data`, and `data.payload` envelopes and recognizes additional H5/backend variants including `sign_enabled`, `enable_sign`, `is_enabled`, `has_signed`, `is_signed`, `has_sign`, `is_sign`, `sign_status`, and `success`.
- Verification after sign-in compatibility hardening: `node --check` passes for `uniapp-src/src/api/live.js` and `uniapp-src/src/pages/broadcast/composables/useLiveSidePanels.js`. A focused forbidden-runtime scan across the changed sign-in-related files reports no browser globals, H5 player packages, JSSDK, or Agora DOM insertion hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/dev/mp-weixin` and `dist/build/mp-weixin` contain the new sign submit aliases and sign status variants. A direct Node module behavior test was not used because local ESM alias resolution for `@/utils` is not configured in plain Node, but source checks and Mini Program builds succeeded.
- Remaining verification boundary is unchanged: this closes a local/generated sign-in payload/status gap, but it still does not prove active production `live-player` playback or remote interaction fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push/sign-in data, and WeChat DevTools plus real-device validation for playback, background/foreground resume, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in submission/status, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 15:02 watch-reward claim-result compatibility hardening without subagents: Trellis `trellis-implement` dispatch was attempted once as required by the workflow, but spawning failed with `agent thread limit reached`; this pass then stayed fully in the main thread and did not retry, wait for, or close subagents. `useLiveWatchRewards` now normalizes successful `/h5/marketing/claimWatchReward` results through the same Mini Program-safe envelope handling used by socket events, while also flattening H5/backend variants such as `record/claimRecord/winnerRecord/result`, `reward/rewardInfo/prize/coupon/product/goods`, `activity/activityInfo/watchReward`, and `userProgress/user_progress/progress`. The normalized result preserves `activityId/activity_id`, defaults missing successful `claimStatus` to `3`, handles `already_claimed`, `need_receiver`, `customer_coupon_id`, `order_no`, snake_case reward fields, and keeps the clicked task as fallback data so the task is removed and the correct H5-copied reward modal can render even when the backend response is sparse or nested.
- Verification after watch-reward claim-result hardening: `node --check uniapp-src/src/pages/broadcast/composables/useLiveWatchRewards.js` passes. A focused forbidden-runtime scan of that file reports no browser globals, H5 player packages, JSSDK, DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/dev/mp-weixin` and `dist/build/mp-weixin` contain `normalizeClaimSuccessPayload`, `explicitClaimStatus`, `already_claimed`, `need_receiver`, and `coupon_customer_id`.
- Remaining verification boundary is unchanged: this closes a local/generated watch-reward claim-success gap, but it still does not prove production watch-reward issuance, active `live-player` playback, or remote interaction fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled barrage/ordinary-lottery/comment-lottery/watch-reward/product-push/sign-in data, and WeChat DevTools plus real-device validation for playback, background/foreground resume, barrage send/display, likes, lottery, watch reward countdown/claim/result, product pushes, report/share, sign-in submission/status, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 15:07 live product shelf response-shape compatibility hardening without subagents: this pass stayed fully in the main thread and did not retry subagent dispatch. `useLiveProducts` now unwraps product list/current-product API responses from top-level, `payload`, `data`, and `data.payload`, and accepts list aliases used by H5/backend pagination shapes: `list`, `records`, `rows`, `items`, `products`, `productList/product_list`, `goodsList/goods_list`, `goods`, and `dataList/data_list`. It also reads nested `page/pager/pagination` list and total fields, and treats a single `product/currentProduct/current_product/goods/currentGoods/current_goods` object as a one-item current explaining product list. This keeps the H5-copied product shelf UI intact while preventing empty shelves when the H5 backend returns a different but equivalent product envelope.
- Verification after product shelf response-shape hardening: `node --check uniapp-src/src/pages/broadcast/composables/useLiveProducts.js` passes. A focused forbidden-runtime scan of that file reports no browser globals, H5 player packages, JSSDK, DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/dev/mp-weixin` and `dist/build/mp-weixin` contain `pickProductResponseList`, `pickProductResponseTotal`, `goods_list`, `data_list`, and `current_product`.
- Remaining verification boundary is unchanged: this closes a local/generated product shelf response parsing gap, but it still does not prove production product-shelf data, current-product push, product click/navigation, active `live-player` playback, or remote interaction fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled product shelf/current-product data plus barrage/ordinary-lottery/comment-lottery/watch-reward/sign-in data, and WeChat DevTools plus real-device validation for playback, product shelf entry, product detail navigation, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 15:15 like interaction payload/WS compatibility hardening without subagents: after the user called out the repeated agent-shutdown stall, this pass stayed fully in the main thread and did not retry, wait for, or close any subagent. `/h5/live/like` HTTP fallback now accepts a context object and sends Mini Program/H5 aliases for `roomId/liveId`, `roomCode`, `tenantId`, `termId/liveTermId`, `customerId/userId`, and `count/likeCount`, including nested `data` aliases for backend parsers that read request bodies differently from the H5 client. `useLiveEntryActions` now passes current room code, tenant, term, and audience id when socket-like sending fails and falls back to HTTP.
- The Mini Program socket adapter and broadcast WS business handler now normalize like totals/deltas from top-level, `data`, `payload`, and `data.payload` shapes, including aliases such as `totalLikes/total_likes`, `likeCount/like_count`, `likes`, `totalLike/total_like`, `likeTotal/like_total`, `zanCount/zan_count`, `likeDelta/like_delta`, `delta`, `count`, `likeCountDelta/like_count_delta`, `addCount/add_count`, and `likeNum/like_num`. This keeps the H5-copied UI intact while preventing valid H5 backend like events from being ignored or treated as a generic `+1` when the server uses a non-primary field name.
- Verification after like compatibility hardening: `node --check` passes for `uniapp-src/src/api/live.js`, `uniapp-src/src/pages/broadcast/composables/useLiveEntryActions.js`, `uniapp-src/src/pages/broadcast/composables/useLiveWsMessageHandler.js`, and `uniapp-src/src/utils/mini-live-socket.js`. A focused forbidden-runtime scan across broadcast, `mini-live-socket.js`, and `api/live.js` reports no browser globals, H5 player packages, JSSDK, or Agora DOM insertion hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited by timeout; no current-workspace watch process remains. Generated dev output contains the new like alias handlers in `pages/broadcast/composables/useLiveWsMessageHandler.js`, `utils/mini-live-socket.js`, and `api/live.js`.
- Remaining verification boundary is unchanged: this closes a local/generated like send/receive compatibility gap, but it still does not prove production remote like fan-out or active `live-player` playback. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled product shelf/current-product data plus barrage/ordinary-lottery/comment-lottery/watch-reward/sign-in data, and WeChat DevTools plus real-device validation for playback, product shelf entry, product detail navigation, barrage send/display, likes, lottery, watch reward, product pushes, report/share, sign-in, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 15:23 lottery/comment-lottery/watch-reward response-envelope compatibility hardening without subagents: Trellis `trellis-implement` dispatch was attempted once for this continuation but failed with `agent thread limit reached`; the pass then stayed in the main thread and did not retry, wait for, or close subagents. The shared `live-lottery-message` unwrapping now merges top-level `payload`, `data`, `data.payload`, `payload.data`, and common `result` envelopes so H5/IM/WS lottery events do not lose activity/prize/winner fields when wrapped differently from the copied H5 path.
- Ordinary lottery now extracts winners and participants from nested response shapes including `records`, `rows`, `items`, `list`, `dataList/data_list`, and nested `page/pager/pagination/result/data/payload`, in addition to H5 names such as `winnerList/winner_list`, `recordList/record_list`, and `participantList/participant_list`. Comment lottery applies the same nested-list handling to activity lists, prize lists, opened-result winners, participant lists, participant API responses, and win-record update payloads, and detail responses are flattened before normalization. Watch reward initialization from live detail now accepts `watchRewards/watch_rewards`, `watchRewardList/watch_reward_list`, `watchRewardActivities/watch_reward_activities`, `watchDurationRewards/watch_duration_rewards`, and generic reward/list/records wrappers under top level or `marketingConfig/marketing_config`.
- Verification after lottery envelope hardening: `node --check` passes for `live-lottery-message.js`, `useLiveNormalLottery.js`, `useLiveCommentLottery.js`, and `useLiveWatchRewards.js`. A focused forbidden-runtime scan across these files reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `XMLHttpRequest`, or `Blob` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited by timeout; generated dev/build output contains `pickNestedList`, `unwrapResponseObject`, `dataPayload`, `watchRewardList`, `watch_reward_list`, and `dataList/data_list` in the expected broadcast composables.
- Remaining verification boundary is unchanged: this closes a local/generated parsing gap for ordinary lottery, comment lottery, and watch reward UI data, but it still does not prove live production activity events, production claim/participation success, or active `live-player` playback. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled product shelf/current-product data plus barrage/ordinary-lottery/comment-lottery/watch-reward/sign-in data, and WeChat DevTools plus real-device validation for playback, product shelf entry, product detail navigation, barrage send/display, likes, ordinary lottery, comment lottery, watch reward countdown/claim/result, report/share, sign-in, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 15:43 active-live source alias/classification hardening without subagents: after the user called out the repeated subagent shutdown stall, this pass did not spawn, close, or wait for subagents. The live playback path now accepts broader H5/backend media source shapes in `utils/live-route.js`, including RTMP/FLV/HLS aliases such as `httpFlvUrl/http_flv_url`, `liveFlvUrl/live_flv_url`, `httpHlsUrl/http_hls_url`, `liveHlsUrl/live_hls_url`, `pullHttp*`, `streamInfos/stream_infos`, `playInfo/play_info`, `mediaInfo/media_info`, `pullInfo/pull_info`, `sourceList/source_list`, `pullUrlList/pull_url_list`, and direct `urls` arrays. Source metadata fields such as `type/protocol/format/source_type/media_type/sourceComponent/playerComponent` are now used to preserve the correct Mini Program component (`live-player` for RTMP/FLV, `video` for HLS/MP4) when URLs are generic or lack a recognizable extension.
- Status and quality-stream playback options were hardened in `pages/broadcast/utils/live-source.js`: nested H5 source/list containers are recursively scanned, string stream arrays are accepted, stream items preserve `sourceType/sourceComponent`, and live status snapshot playback options now pass the matched candidate component/type through to `initVideoPlayer`. `live-entry-initializer-helpers.js` now uses the same snake_case/camelCase/httpFlv/httpHls fallback aliases before initializing active-live playback, and `useLivePlayerInitializer.js` preserves component metadata on fallback URLs and records the final normalized media component in debug logs.
- Verification after active-live source alias/classification hardening: `node --check` passes for `utils/live-route.js`, `pages/broadcast/utils/live-source.js`, `live-entry-initializer-helpers.js`, and `useLivePlayerInitializer.js`. A focused forbidden-runtime scan across broadcast playback files reports no browser globals, H5 player packages, JSSDK, or Agora DOM insertion hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated dev output contains the new `httpFlvUrl`, `streamInfos`, `pullUrlList`, `sourceComponent`, and `playerComponent` handling, and no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` watch process remains.
- Remaining verification boundary is unchanged: this closes a local/generated active-live playback-source parsing and component-classification gap, but it still does not prove production `live-player` playback on a real device. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable RTMP/FLV/HLS, valid auth, configured request/socket/upload/download/live-player domains, and WeChat DevTools plus real-device validation for playback, background/foreground resume, barrage send/display, likes, product shelf, ordinary lottery, comment lottery, watch reward, sign-in, report/share, complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 15:43 Mini Program-native share entry hardening without subagents: `trellis-implement` dispatch was attempted once as required by the workflow but failed with `agent thread limit reached`; this pass then stayed fully in the main thread and did not retry, wait for, or close subagents. Broadcast file parity was rechecked against `../live_h5/src/pages/broadcast`: all H5 broadcast components, composables, styles, and utils have corresponding files in `uniapp-src/src/pages/broadcast` (target has additional Mini Program static assets and adapters). The next concrete viewer-loop gap fixed was the live-room share popup: H5 uses JSSDK/menu guidance, while Mini Program sharing must be triggered through a native `button open-type="share"`. `uniapp-src/src/components/share-popup.vue` now keeps the copied H5 share item class/layout but renders the WeChat share item as an `open-type="share"` button once distributor share URL loading is complete, emits the same share metadata to update `shareCode`, calls `showShareMenu`, and closes the popup after the native share trigger. During share URL loading it keeps the H5-looking item as a non-share view and prompts the user to tap again after the attribution link is ready, avoiding a race where Mini Program share opens before distributor/replay parameters are prepared.
- Verification after Mini Program share hardening: focused forbidden-runtime scan across broadcast, share popup, and Mini Program socket files reports no browser globals, H5 player packages, JSSDK, or Agora DOM insertion hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated `dist/dev/mp-weixin/components/share-popup.wxml` and `dist/build/mp-weixin/components/share-popup.wxml` contain the native `open-type="share"` button, and generated JS contains `onMiniProgramWechatShare` plus the loading prompt. No current-workspace `dev:mp-weixin` / `uni -p mp-weixin` watch process remains after cleanup.
- Remaining verification boundary is unchanged: this closes a local/generated share-trigger gap, but it still does not prove real WeChat share callback behavior, active production `live-player` playback, or remote interaction fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled product shelf/current-product data plus barrage/ordinary-lottery/comment-lottery/watch-reward/sign-in data, and WeChat DevTools plus real-device validation for playback, barrage send/display, likes, product shelf/detail entry, ordinary lottery, comment lottery, watch reward, sign-in, native share, report/complaint submission, replay sharing, and replay/live switching.

- 2026-06-03 15:53 complaint/report room-context compatibility hardening without subagents: after the user called out the repeated subagent shutdown stall, this pass did not spawn, close, or wait for any subagent. The live-room report popup and report secondary pages now preserve H5-compatible live context through upload and submit: `roomId/room_id/liveId/live_id`, `roomCode/room_code`, `tenantId/tenant_id`, `termId/term_id/liveTermId/live_term_id`, `customerId/customer_id/userId/user_id`, replay flags, `replayVideoId/replay_video_id/videoId/video_id`, `liveType/live_type`, `liveName/live_name/roomName/room_name`, cover aliases, and `fromPath/from_path/sourcePath/source_path/returnPath/return_path`. `api/complaint.js` now normalizes the same aliases for upload URL acquisition, complaint creation, list, and detail calls. `live-report-popup.vue`, `report-home.vue`, `report-type.vue`, and `report-form.vue` pass the full room context instead of only `liveId/roomCode`, so complaint image uploads and submissions no longer lose tenant, term, user, replay, or return-to-live metadata.
- Verification after complaint/report hardening: `node --check uniapp-src/src/api/complaint.js` passes. A focused forbidden-runtime scan across complaint/report, live report popup, and portrait/landscape broadcast stages reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, or H5 upload APIs. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/dev/mp-weixin` and `dist/build/mp-weixin` contain the new `tenant_id`, `replay_video_id`, `from_path`, `source_path`, and `live_name` complaint/report fields.
- Remaining verification boundary is unchanged: this closes a local/generated complaint submission context gap, but it still does not prove backend acceptance of the exact complaint payload, upload-domain behavior on device, active production `live-player` playback, or remote interaction fan-out. Final completion still requires a currently active `pushStatus: 1` room with Mini Program-playable media, valid auth, configured request/socket/upload/download/live-player domains, enabled product shelf/current-product data plus barrage/ordinary-lottery/comment-lottery/watch-reward/sign-in data, and WeChat DevTools plus real-device validation for playback, product shelf/detail entry, barrage send/display, likes, lottery, watch reward, sign-in, native share, complaint image upload/submission, replay sharing, and replay/live switching.

- 2026-06-03 16:35 Mini Program playback context/ready-event hardening without subagents: Trellis `trellis-implement` dispatch was attempted once but spawning still failed with `agent thread limit reached`; this pass then stayed in the main thread and did not retry, wait for, or close any subagent. The current user-reported DevTools log showed the live detail, history comments, and WebSocket all initialized, then `enterLive` followed by a generic Mini Program `Error: timeout`, so this pass focused on the native component command path rather than changing H5-copied visuals. `useLivePlayerInitializer.updateMiniProgramMuted()` now respects the active media component: when the current source is rendered through `live-player`, unmute/resume no longer first creates or plays a `video` context for the same `liveVideo` id. This avoids sending user-gesture playback commands to the wrong Mini Program component, a plausible source of DevTools context timeouts when the stage renders `<live-player>` for RTMP/FLV active live streams.
- Replay/HLS readiness was also hardened without changing layout: both `LivePortraitStage.vue` and `LiveLandscapeStage.vue` now listen for the Mini Program `video` `loadedmetadata` event in addition to `play`, `playing`, `loadeddata`, and `timeupdate`. This gives the copied H5 stage a native Mini Program ready signal for HLS/MP4 sources that may not emit `loadeddata` before the existing playback-ready logic decides whether to clear failure state.
- Verification after playback context/ready-event hardening: focused forbidden-runtime scan of `LivePortraitStage.vue`, `LiveLandscapeStage.vue`, and `useLivePlayerInitializer.js` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/dev/mp-weixin` contains `bindloadedmetadata` for both portrait and landscape `video` nodes, plus the updated `updateMiniProgramMuted` implementation.
- Remaining verification boundary is still open: this closes a local/generated playback-command mismatch and ready-event coverage gap, but it does not prove the current active room now plays in WeChat DevTools or on device. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin` against an active room and validating live `live-player` playback, replay `video` playback, barrage send/display, likes, product shelf, lottery, watch reward, sign-in, report/share, and replay/live switching on DevTools plus real device.

- 2026-06-03 16:49 Mini Program live-player resume/unmute loop hardening without subagents: this continuation stayed in the main thread because subagent spawning is still blocked by the known thread-limit condition. The next playback risk was in the H5-derived sound-intent loop: Mini Program runtime has no DOM video element, so `scheduleLiveSoundIntentRestore()` could keep `_pendingUnmute` true until the retry limit because its success condition only trusted `el.muted === false`. It now treats the Mini Program player state as sufficient when there is no DOM element (`videoPlayer.muted === false` and `isMuted.value === false`) and clears `_pendingUnmute` at the retry limit. This prevents repeated `enterLive`/`onShow` sound-restore attempts from repeatedly issuing native player commands after the local muted state already changed.
- Native resume/playback command selection was also tightened. `updateMiniProgramMuted()` now calls `live-player` `play()` before `resume()` when unmuting a live-player source, aligning with the WeChat `LivePlayerContext` API where `mute()` exists but unmute is primarily driven by the component `muted` prop plus `play()`. `useLiveVideoRuntime.tryPlayNativeContext()` now resolves the known media component from replay state, `mediaSourceComponent`, or the current player metadata; when the component is known, it only commands that component instead of trying `video` and `live-player` fallbacks for the same `liveVideo` id. Unknown component cases still keep the old fallback order.
- Verification after resume/unmute loop hardening: `node --check` passes for `useLiveSoundIntent.js`, `useLivePlayerInitializer.js`, and `useLiveVideoRuntime.js`. A focused forbidden-runtime scan of those files reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/dev/mp-weixin` contains `getKnownMediaComponent`, `_pendingUnmute = false` cleanup, and `liveCtx.play()` in the expected broadcast composables.
- Remaining verification boundary is still open: this reduces native context timeout risk and repeated sound-restore commands, but it still does not prove the active room renders video on WeChat DevTools or a real device. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin`, validating active `live-player` playback with a currently live room, validating replay `video` playback, and running the viewer-loop checks for barrage send/display, likes, product shelf, ordinary lottery, comment lottery, watch reward, sign-in, report/share, and replay/live switching.

- 2026-06-03 17:05 barrage/comment live-context propagation hardening without subagents: this continuation stayed fully in the main thread and did not attempt to spawn, close, or wait for subagents. `useLiveComments` now receives current `tenantId`, `shareCode`, `bindId`, and live/replay mode from `entry.vue`, and includes both camelCase and snake_case H5 aliases in comment history, HTTP comment fallback, and WebSocket chat payloads. `api/live.js` and `mini-live-socket.js` now preserve these same aliases at top level and inside nested `data`, so barrage send/display no longer depends on a backend parser accepting only room/customer/content fields while ignoring tenant/share/bind/liveType context.
- Verification after barrage/comment live-context propagation hardening: `node --check` passes for `useLiveComments.js`, `api/live.js`, and `mini-live-socket.js`. A focused forbidden-runtime scan of those files reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated dev output contains `tenantId/tenant_id`, `shareCode/share_code`, `bindId/bind_id`, and `liveType/live_type` in `entry.js`, `useLiveComments.js`, `api/live.js`, and `mini-live-socket.js`.
- Remaining verification boundary is still open: this closes another generated-package barrage context gap, but it does not prove backend acceptance or production socket fan-out. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin`, validating active `live-player` playback with a currently live room, validating replay `video` playback, and running the viewer-loop checks for barrage send/display, likes, product shelf, ordinary lottery, comment lottery, watch reward, sign-in, report/share, and replay/live switching on WeChat DevTools plus real device.

- 2026-06-03 17:22 live sign-in context propagation hardening without subagents: Trellis Phase 2.1 sub-agent dispatch was attempted once in this session but failed with `agent thread limit reached`, so this pass stayed on the main thread and did not retry, wait for, or close subagents. The H5 source confirms sign-in uses `/h5/live/sign/check` and `/h5/live/sign/submit`; the migrated Mini Program UI already preserves the H5 `live-sign-in` component shape in portrait popup and landscape tab, but the runtime context was incomplete. `api/live.js` now treats `bindId/bind_id` and `liveType/live_type` as first-class live aliases, and sign submit nested `data` now preserves `tenantId/tenant_id`, `shareCode/share_code`, `bindId/bind_id`, and `liveType/live_type`. `components/live-sign-in.vue` accepts those props and forwards them to `submitLiveSign`. `LivePortraitStage.vue` and `LiveLandscapeStage.vue` pass current tenant/share/bind/live-mode into the H5-copied sign component, and `useLiveSidePanels.loadSignStatus()` sends the same context during the sign-status check.
- Verification after live sign-in context propagation hardening: `node --check` passes for `uniapp-src/src/api/live.js` and `uniapp-src/src/pages/broadcast/composables/useLiveSidePanels.js`. A focused forbidden-runtime scan across `api/live.js`, `components/live-sign-in.vue`, portrait/landscape broadcast stages, `useLiveSidePanels.js`, and `entry.vue` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated dev output contains the new sign-in context fields in `components/live-sign-in.js`, `pages/broadcast/components/LivePortraitStage.wxml`, `pages/broadcast/components/LiveLandscapeStage.wxml`, `pages/broadcast/composables/useLiveSidePanels.js`, `api/live.js`, and `pages/broadcast/entry.js`.
- Remaining verification boundary is still open: this closes a generated-package sign-in context gap, but it does not prove backend acceptance of the sign check/submit payload or the real sign-in interaction in WeChat DevTools/device. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin`, validating active `live-player` playback with a currently live room, validating replay `video` playback, and running the viewer-loop checks for barrage send/display, likes, product shelf, ordinary lottery, comment lottery, watch reward, sign-in, report/share, and replay/live switching on WeChat DevTools plus real device.

- 2026-06-03 17:39 watch-duration reward claim context propagation hardening without subagents: Trellis Phase 2.1 sub-agent dispatch was attempted once in this continuation but failed with `agent thread limit reached`; this pass stayed on the main thread and did not retry or wait on subagents. H5 confirms watch reward claims post to `/h5/marketing/claimWatchReward`. The migrated Mini Program runtime already preserved the H5 watch-reward modal and entry UI, but the claim payload only carried room/term/customer fields. `useLiveMarketingRuntime` now passes `liveTenantId`, `shareCode`, `liveBindId`, and replay/live mode into `useLiveWatchRewards`. `claimWatchRewardTask()` now sends `tenantId/tenant_id`, `shareCode/share_code`, `bindId/bind_id`, `liveType/live_type`, plus current progress aliases `watchDuration/watch_duration`, `watchedSec/watched_sec`, `thresholdSec/threshold_sec`, and `remainingSec/remaining_sec`. `api/marketing.js` now treats these as marketing aliases and mirrors them into nested `data`, protecting backends that parse either top-level H5 body fields or nested data.
- Verification after watch-duration reward claim context propagation hardening: `node --check` passes for `uniapp-src/src/api/marketing.js`, `uniapp-src/src/pages/broadcast/composables/useLiveMarketingRuntime.js`, and `uniapp-src/src/pages/broadcast/composables/useLiveWatchRewards.js`. A focused forbidden-runtime scan across those files plus `entry.vue` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated dev output contains the new watch-reward claim context/progress fields in `api/marketing.js`, `useLiveMarketingRuntime.js`, `useLiveWatchRewards.js`, and `entry.js`.
- Remaining verification boundary is still open: this closes a generated-package watch-reward claim payload gap, but it does not prove production backend acceptance, successful reward issuance, broadcast fan-out, or the visible modal/result behavior on device. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin`, validating active `live-player` playback with a currently live room, validating replay `video` playback, and running the viewer-loop checks for barrage send/display, likes, product shelf, ordinary lottery, comment lottery, watch reward countdown/claim/result/broadcast, sign-in, report/share, and replay/live switching on WeChat DevTools plus real device.

- 2026-06-03 16:41 ordinary/comment lottery context propagation hardening without subagents: this continuation stayed in the main thread after the known `agent thread limit reached` condition and did not retry, wait on, or close subagents. The H5 source confirms ordinary lottery participant lookup uses `/h5/marketing/lottery/participants`, while comment lottery uses `/h5/marketing/commentLottery/list`, `/h5/marketing/commentLottery/detail`, and `/h5/marketing/claimCommentReward`. The migrated Mini Program UI already had the H5-copied lottery surfaces, but ordinary/comment lottery API calls only carried room/term/customer/roomCode. `useLiveMarketingRuntime` now passes `liveTenantId`, `shareCode`, `liveBindId`, and replay/live mode into both `useLiveNormalLottery` and `useLiveCommentLottery`. Both composables now send camelCase and snake_case aliases for `tenantId`, `shareCode`, `bindId`, and `liveType`. Ordinary lottery participant requests also carry `activityType/activity_type` and `winType/win_type` from the incoming H5 lottery payload, falling back to ordinary lottery type. Comment lottery list/detail/participants/claim requests carry comment-lottery `activityType/activity_type` and `winType/win_type`, while preserving the existing `comment/content` submission aliases.
- Verification after ordinary/comment lottery context propagation hardening: `node --check` passes for `uniapp-src/src/pages/broadcast/composables/useLiveNormalLottery.js`, `uniapp-src/src/pages/broadcast/composables/useLiveCommentLottery.js`, and `uniapp-src/src/pages/broadcast/composables/useLiveMarketingRuntime.js`. A focused forbidden-runtime scan across those files plus `uniapp-src/src/api/marketing.js` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout with code 124 as expected; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated dev output contains the new lottery context and type aliases in `useLiveNormalLottery.js`, `useLiveCommentLottery.js`, and `useLiveMarketingRuntime.js`.
- Remaining verification boundary is still open: this closes a generated-package lottery payload-context gap, but it does not prove backend acceptance of participant/list/detail/claim requests, socket fan-out of lottery events, visible slot/rolling/result modal behavior, or active production playback. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin`, validating active `live-player` playback with a currently live room, validating replay `video` playback, and running the viewer-loop checks for barrage send/display, likes, product shelf, ordinary lottery, comment lottery, watch reward countdown/claim/result/broadcast, sign-in, report/share, and replay/live switching on WeChat DevTools plus real device.

- 2026-06-03 16:51 live socket/like context propagation hardening without subagents: Trellis Phase 2.1 `trellis-implement` dispatch was attempted once at the start of this continuation but still failed with `agent thread limit reached`; this pass then stayed in the main thread and did not retry, wait on, or close subagents. The H5 live room batches likes for 3 seconds and sends `/h5/live/like`, while socket events also carry enter/leave/like/chat semantics. The migrated Mini Program HTTP like adapter already supported H5 aliases, but the action caller and `MiniLiveSocket` did not consistently propagate `tenantId`, `shareCode`, `bindId`, `liveType`, term, and customer context. `useLiveEntryActions` now passes those aliases to both socket `sendLike` and HTTP fallback. `useMessageChannel` and `useLiveWebSocket` now forward a canonical live-room context into `MiniLiveSocket`, including room/live id aliases, term aliases, customer/user aliases, tenant/share/bind, and live/replay mode. `MiniLiveSocket` stores this context and automatically merges it into outgoing `send`, `sendChat`, `sendLike`, `sendEnter`, and `sendLeave` payloads at top level and inside nested `data`, while still allowing per-call payloads such as replay chat metadata or like count to override where needed.
- Verification after live socket/like context propagation hardening: `node --check` passes for `uniapp-src/src/pages/broadcast/composables/useLiveWebSocket.js`, `uniapp-src/src/pages/broadcast/composables/useMessageChannel.js`, `uniapp-src/src/pages/broadcast/composables/useLiveEntryActions.js`, and `uniapp-src/src/utils/mini-live-socket.js`. A focused forbidden-runtime scan across those files plus `entry.vue` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout with code 124 as expected; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated dev output contains `buildLiveSocketContext`, `normalizeSocketContext`, context merges in `sendChat`/`sendLike`/`sendEnter`/`sendLeave`, and the updated `liveSocket.sendLike(count, context)` call.
- Remaining verification boundary is still open: this closes a generated-package context gap for socket upflow and like submission, but it does not prove backend acceptance, production socket fan-out, visible like counter synchronization, or active production playback. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin`, validating active `live-player` playback with a currently live room, validating replay `video` playback, and running the viewer-loop checks for barrage send/display, likes, product shelf, ordinary lottery, comment lottery, watch reward countdown/claim/result/broadcast, sign-in, report/share, and replay/live switching on WeChat DevTools plus real device.

- 2026-06-03 17:09 live product shelf/detail/buy-reminder context propagation hardening without subagents: Trellis Phase 2.1 `trellis-implement` dispatch was attempted once but failed with `agent thread limit reached`; this pass then stayed in the main thread and did not retry agent spawning. The product shelf and current explaining product calls now carry H5 live-room context from `entry.vue` into `useLiveProducts`, including `roomCode/room_code`, `tenantId/tenant_id`, `shareCode/share_code`, `bindId/bind_id`, `liveType/live_type`, `termId/liveTermId`, `customerId/userId`, and replay video aliases. `useLivePurchase` now sends the same context for buy reminders and product-detail entry, while preserving product/goods/sku aliases and product name/image aliases. `api/live.sendBuyReminder()` mirrors the new fields into nested `data`, and `api/product.getProductDetail()` now aliases `liveType`, `customerId/userId`, and `videoId/replayVideoId` for H5 backend parsers that require them.
- Verification after product shelf/detail/buy-reminder context hardening: `node --check` passes for `uniapp-src/src/api/live.js`, `uniapp-src/src/api/product.js`, `uniapp-src/src/pages/broadcast/composables/useLiveProducts.js`, and `uniapp-src/src/pages/broadcast/composables/useLivePurchase.js`. A focused forbidden-runtime scan across those files plus `entry.vue` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout with code 124 as expected; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated dev output contains `shareCode/share_code`, `bindId/bind_id`, `liveType/live_type`, and `replayVideoId/replay_video_id` in `api/live.js`, `api/product.js`, and `pages/broadcast/entry.js`.
- Remaining verification boundary is still open: this closes a generated-package context gap for product shelf/current-product/product-detail/buy-reminder calls, but it does not prove backend acceptance, visible product shelf population, current-product push synchronization, product-detail popup data, or active production playback. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin`, validating active `live-player` playback with a currently live room, validating replay `video` playback, and running the viewer-loop checks for barrage send/display, likes, product shelf, product detail entry, ordinary lottery, comment lottery, watch reward countdown/claim/result/broadcast, sign-in, report/share, and replay/live switching on WeChat DevTools plus real device.
- 2026-06-03 17:38 live playback source selection hardening without subagents: after the user explicitly requested no more subagents, this pass stayed in the main thread and did not spawn, wait for, or close agents. `LivePortraitStage.vue` and `LiveLandscapeStage.vue` already contained the completed live-player readiness fix (`2003` ready state plus uppercase/underscore netstatus fields). This pass hardened `selectMiniProgramLiveCandidate()` so WeChat DevTools still prefers `video`/HLS-compatible candidates, iOS can explicitly prefer HLS/video, and real Mini Program runtime prefers native `live-player` RTMP/FLV candidates before falling back to HLS/video. `buildLivePlayerSource()` and live-status snapshot source resolution now pass the same `preferHls` intent into candidate selection, reducing cases where the detail init path and status-update path choose different source components for the same room.
- Verification after live playback source selection hardening: `node --check` passes for `uniapp-src/src/pages/broadcast/utils/live-source.js` and `uniapp-src/src/pages/broadcast/composables/live-entry-initializer-helpers.js`. A focused forbidden-runtime scan across the changed source-selection files, portrait/landscape stages, `useLivePlayerInitializer.js`, and `useLiveSoundIntent.js` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout with code 124 as expected; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated dev/build output contains the new `preferHls` candidate selection, `2003` live-player ready code, and uppercase netstatus support.
- Remaining verification boundary is still open: this reduces source-component mismatch and live-player readiness false-negative risk, but it does not prove active production playback. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin`, validating active `live-player` playback with a currently live room, validating replay `video` playback, and running the viewer-loop checks for barrage send/display, likes, product shelf, ordinary lottery, comment lottery, watch reward countdown/claim/result/broadcast, sign-in, report/share, and replay/live switching on WeChat DevTools plus real device.
- 2026-06-03 17:45 viewer-loop route registration hardening without subagents: this pass stayed entirely in the main thread per the user's instruction not to use subagents. A route coverage script found that several real viewer-loop pages existed but were not registered in `uniapp-src/src/pages.json`, so live-room and personal-center navigation could fail at runtime even though the target `.vue` files existed. `pages.json` now registers old live compatibility redirects (`pages/live/live-vertical`, `pages/live/live-horizontal`), legacy share-live redirects (`pages/agent/shareLive/shareLive`, `pages/agent/shareLive/live-detail`), and the H5-oriented order/refund loop pages (`pages/order/confirm`, `list`, `detail`, `pay`, `receipt`, `refund`, `refund-list`, `refund-detail`, refund nested redirect aliases, `confirm-order`, `order-detail`, `myorder`, `cashier`, `pay-success/pay-success`, `pay-h5/pay-h5`, `alipay-h5/alipay-h5`, and `deposit-pay`). This closes a registration-level gap where valid migrated pages could still be unreachable from live-room product, prize, center, and legacy link flows.
- Verification after route registration hardening: `pages.json` parses as valid JSON. A route coverage script confirms registered + matching `.vue` for broadcast, old live redirects, share-live redirects, center, order/refund aliases, address, report, prize-record, and invitation-record routes. A focused empty-shell keyword scan across the critical order/refund and old live redirect pages reports no `TODO:migration`, `暂未开放`, `待迁移`, or `not implemented` hits. `npm run build:mp-weixin` passes with only existing Sass/Wot Design deprecation warnings. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout with code 124 as expected; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/dev/mp-weixin/app.json` and `dist/build/mp-weixin/app.json` both contain the newly registered live/share/order/refund routes.
- Remaining verification boundary is still open: route registration makes the viewer-loop pages reachable, but it does not prove each page's backend calls, H5 visual parity, or active production live-room behavior on device. Final completion still requires WeChat DevTools plus real-device validation for live/replay playback, barrage, likes, product shelf/detail/order entry, sign-in, ordinary/comment lottery, watch reward, report/share, center modules, and return-to-live.

- 2026-06-03 18:04 active-room source and WS evidence pass without subagents: per user instruction, this pass stayed in the main thread and did not spawn, wait for, or close any subagents. Using the provided test room context, `/h5/live/detail` returned `pushStatus=1`, `displayMode=2`, `roomId=232`, RTMP/FLV/HLS live URLs, `adaptiveHlsUrl`, one replay item, and marketing config. `/h5/live/streamInf` returned the same live stream family, `/h5/live/replayFirstVideo` returned a replay HLS video, and `/h5/live/wsSignKey` returned a signing key. A local non-chat WebSocket probe using the source debug token connected to `wss://man.lqjy.cc/api/h5/live/ws?roomId=232`, sent a signed ping envelope, and received a server product-list event (`type=18`), proving token, WS URL, and HMAC envelope compatibility for the fallback Mini Program socket path without sending a public barrage.
- Source-selection hardening from this pass: `utils/live-route.js`, `pages/broadcast/composables/live-entry-initializer-helpers.js`, and `pages/broadcast/utils/live-source.js` now recognize `adaptiveHlsUrl/adaptive_hls_url` plus `liveAdaptiveHlsUrl/live_adaptive_hls_url` as HLS candidates. Non-iOS generic fallback now prefers explicit FLV/HLS before unknown `pullUrl`, while RTMP still wins through candidate selection for real Mini Program runtime and HLS/video still wins for DevTools/iOS where configured. This matches the observed active room response shape where top-level RTMP/FLV/HLS and adaptive HLS coexist.
- Verification after active-room source/WS evidence pass: `node --check` passes for `uniapp-src/src/utils/live-route.js`, `uniapp-src/src/pages/broadcast/composables/live-entry-initializer-helpers.js`, and `uniapp-src/src/pages/broadcast/utils/live-source.js`. A focused forbidden-runtime scan across broadcast source, `utils/live-route.js`, `utils/mini-live-socket.js`, and `api/live.js` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes with only existing Sass deprecation warnings. `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin`; generated dev/build output contains the new adaptive HLS aliases and adjusted fallback order. The transient dev watch child process left by `timeout` was explicitly killed and no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains.
- Remaining verification boundary is still open: local API and WS evidence is stronger than static checks, but final completion still requires reloading `uniapp-src/dist/dev/mp-weixin` in WeChat DevTools and validating real `live-player` video rendering, replay `video` rendering, barrage send/display (not just socket connect), likes, product shelf/detail, sign-in, ordinary/comment lottery, watch reward countdown/claim/result/broadcast, report/share, replay/live switching, and real-device behavior.

- 2026-06-03 18:14 live-player ready/netstatus state hardening without subagents: per the user's current instruction, this pass stayed in the main thread and did not spawn, wait for, or close subagents. The live playback chain was rechecked from source selection through `displayVideoUrl`, `mediaSourceComponent`, portrait/landscape `live-player`/`video` binding, and first-frame/poster gating. A concrete risk was found in duplicated native `live-player` readiness logic: portrait and landscape each recognized only a limited netstatus field set, so a valid native stream could keep the H5-copied cover/error gating active if WeChat reported activity through other common fields. A new shared `pages/broadcast/utils/live-player-status.js` now owns `LIVE_PLAYER_READY_CODES` and `hasLivePlayerNetActivity()`, adds common Mini Program live-player netstatus aliases such as `videoFps`, `videoCache`, `audioCache`, `videoGOP/videoGop`, and `avRecvInterval`, and both `LivePortraitStage.vue` and `LiveLandscapeStage.vue` import the shared helper. This keeps the copied H5 visual structure intact while making live-player first-frame detection more consistent across portrait and landscape.
- Verification after live-player state hardening: `node --check uniapp-src/src/pages/broadcast/utils/live-player-status.js` passes. Direct `node --check` on `.vue` files is not applicable because Node reports unknown `.vue` extension, so SFC verification was performed through `npm run build:mp-weixin`, which passes. A focused scan confirms the duplicated local `LIVE_PLAYER_NET_ACTIVITY_FIELDS`, `firstNumericField`, and `hasLivePlayerNetActivity` implementations were removed from both stage components and only the shared utility remains. A focused forbidden-runtime scan across broadcast source, `utils/live-route.js`, `utils/mini-live-socket.js`, `api/live.js`, and `api/marketing.js` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout; the transient watch child processes were killed and no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated dev/build output contains `pages/broadcast/utils/live-player-status.js`, the shared imports in both stage components, and the new netstatus aliases.
- Remaining verification boundary is still open: this reduces native live-player first-frame false-negative risk and keeps portrait/landscape behavior aligned, but it does not prove actual media rendering in WeChat. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin` in WeChat DevTools and validating active `live-player` video rendering, replay `video` rendering, barrage send/display, likes, product shelf/detail, sign-in, ordinary/comment lottery, watch reward countdown/claim/result/broadcast, report/share, replay/live switching, and real-device behavior.

- 2026-06-03 18:23 live-entry user-gesture resume hardening without subagents: this continuation stayed on the main thread and did not spawn, wait for, or close any subagents. Broadcast source coverage was rechecked with a valid shell `find`: all 77 H5 runtime `.vue/.js/.scss` files under `live_h5/src/pages/broadcast/` have corresponding Mini Program implementations; Mini Program extras are platform adapters (`utils/domain-ready.js`, `utils/live-route-context.js`, `utils/live-player-status.js`). The product-list purchase-atmosphere rotation was also compared against H5 and confirmed to be source-faithful rather than an MP-only mock deviation. The next concrete risk found was the Mini Program branch of `useLiveSoundIntent.enterLive()`: if the viewer tapped the entry overlay before `videoPlayer` had been initialized, the old non-H5 path only marked `_pendingUnmute = true`; unlike H5, there is no DOM video element to play directly, so the native `live-player`/`video` resume path could be missed in that user-gesture window. The non-H5 branch now immediately calls `resumeVideoPlayback()` while preserving pending unmute retry, so tapping into the room can trigger native playback even when the player wrapper is still being created.
- Verification after live-entry user-gesture resume hardening: `node --check uniapp-src/src/pages/broadcast/composables/useLiveSoundIntent.js` passes. A focused forbidden-runtime scan across broadcast source, `utils/live-route.js`, `utils/mini-live-socket.js`, `api/live.js`, and `api/marketing.js` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated `dist/dev/mp-weixin/pages/broadcast/composables/useLiveSoundIntent.js` contains the updated no-player branch with `resumeVideoPlayback()`, and build output contains the same behavior in minified form.
- Remaining verification boundary is still open: this reduces a real native playback-start timing risk, but it does not prove actual live media rendering or interaction success. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin` in WeChat DevTools and validating active `live-player` video rendering, replay `video` rendering, barrage send/display, likes, product shelf/detail, sign-in, ordinary/comment lottery, watch reward countdown/claim/result/broadcast, report/share, replay/live switching, and real-device behavior.

- 2026-06-03 18:28 replay progress context hardening without subagents: this pass stayed on the main thread and did not spawn, wait for, or close subagents. The replay flow was rechecked against H5: H5 uses `navigator.sendBeacon` on page leave for `/h5/live/reportViewProgress`, while the Mini Program adaptation correctly uses the H5 API client because Mini Program has no browser beacon/Blob path. A context gap was found in the adapted replay progress report: unlike other migrated viewer interactions, `flushViewProgressBeacon()` only sent room/term/video/progress fields, so the H5 backend could miss roomCode, tenant, share/bind attribution, replay/live type, and current customer/user aliases. `useLiveProgressReport` now accepts `roomCode`, `liveTenantId`, `shareCode`, `liveBindId`, and `myUserId` from `entry.vue` and sends camelCase plus snake_case aliases with `liveType/live_type: replay`. `api/live.reportViewProgress()` now preserves caller-supplied context fields before normalizing required numeric progress fields.
- Verification after replay progress context hardening: `node --check` passes for `uniapp-src/src/api/live.js` and `uniapp-src/src/pages/broadcast/composables/useLiveProgressReport.js`. A focused forbidden-runtime scan across broadcast source, `utils/live-route.js`, `utils/mini-live-socket.js`, `api/live.js`, and `api/marketing.js` reports no browser globals, H5 player packages, JSSDK, Agora DOM insertion, `fetch`, `Blob`, or `XMLHttpRequest` hits. `npm run build:mp-weixin` passes. `npm run dev:mp-weixin` refreshed the user-imported `dist/dev/mp-weixin` package and exited by timeout; no current-workspace `dev:mp-weixin` / `uni -p mp-weixin` process remains. Generated dev/build output contains the replay progress context aliases in `pages/broadcast/composables/useLiveProgressReport.js` and the preserved context payload in `api/live.js`.
- Remaining verification boundary is still open: this closes a generated-package replay progress attribution gap, but it does not prove backend acceptance or visible replay behavior. Final completion still requires reloading `uniapp-src/dist/dev/mp-weixin` in WeChat DevTools and validating active `live-player` video rendering, replay `video` rendering/progress, replay simulated messages, barrage send/display, likes, product shelf/detail, sign-in, ordinary/comment lottery, watch reward countdown/claim/result/broadcast, report/share, replay/live switching, and real-device behavior.

- 2026-06-03 18:38 replay simulated-order context propagation hardening: stayed on the main thread while trellis-implement runs a bounded side task. The Mini Program replay simulated-order path now carries H5 live-room context into /h5/live/replaySimMessages: room/live id aliases, roomCode, tenant, shareCode, bindId, term/liveTerm, customer/user, liveType=replay, plus start_sec/end_sec aliases. useReplaySimOrders stores this context per video and reuses it for subsequent 20s preload windows, while useLiveReplayPlayback builds the context from the active replay video and entry refs. This preserves the H5 playback/timeline structure but avoids losing backend attribution/filtering context in Mini Program replay buying-notice simulation. Verification: node --check passed for api/live.js, useReplaySimOrders.js, useLiveReplayPlayback.js, and useLivePlaybackWiring.js; focused forbidden-runtime scan across broadcast/live api/socket/marketing returned no hits; npm run build:mp-weixin passed; npm run dev:mp-weixin refreshed dist/dev/mp-weixin and generated output contains buildReplaySimContext, replaySimMessages, share_code, live_type, and start_sec. The dev watch processes were cleaned up after timeout. Remaining verification boundary: DevTools/real-device playback and viewer-loop interaction validation are still required.

- 2026-06-03 18:40 viewer-loop static acceptance script rerun: fixed the local verification script to include subPackages when resolving pages.json route registration. The corrected check covers 22 viewer-loop routes including broadcast entry/replay, old live redirects, user/center, order/refund subpackage routes, address, report, prize record, and invitation record. Result: 196 registered routes discovered, no missing route/file pairs, all 77 H5 broadcast runtime files still have corresponding Mini Program files, and no empty-shell keywords were found in critical broadcast/center/address/report/prize/invitation directories. This is still static evidence only; DevTools and real-device behavior remain required.

- 2026-06-03 18:42 route normalization smoke check: source and generated dev output were inspected for old live redirects and broadcast replay redirect. Fixed-input ESM smoke tests passed for scene=live_id:123, live_id=123, roomCode+tenantId+_tc, replay=1+video_id, and mode=replay+room_id, all resolving to /pages/broadcast/entry with the expected liveId/roomCode/replay/videoId/liveType query parameters. Old /pages/live/live-vertical and /pages/live/live-horizontal remain thin redirect pages to broadcast with vertical/horizontal orientation preserved.

- 2026-06-03 18:52 live-center secondary-route context hardening: added shared live-room context normalization/query helpers so viewer-loop secondary pages receive complete broadcast context instead of only roomCode. The helpers preserve room/live id, roomCode, tenant, shareCode, bindId, term/liveTerm, customer/user, replay/liveType, and replay video aliases, with explicit live state preventing stale replay video cache pollution. Center pages and the live-room center popup now use appendLiveRoomQuery for orders, refunds, prize record, invitation record, complaint, and address routes; returnToLiveRoom also rebuilds /pages/broadcast/entry from cached + explicit context. This preserves the Mini Program personal-center visual shell while making H5-oriented secondary module navigation and return-to-live less likely to lose attribution/replay state. Verification: node --check passed for live-room-context.js, live-room-navigation.js, and useLiveSidePanels.js; URL smoke checks passed for replay and explicit live context; npm run build:mp-weixin passed; npm run dev:mp-weixin refreshed dist/dev/mp-weixin; generated output contains appendLiveRoomQuery/mergeLiveRoomContext and replayVideoId propagation in center/user/broadcast side-panel files. No current-workspace watch process remains. Remaining verification boundary: DevTools/real-device navigation and backend acceptance for these pages remain required.

- 2026-06-03 19:00 narrow parallel broadcast visual-structure check: compared `/Users/apple/Desktop/code/live_h5/src/pages/broadcast` with `uniapp-src/src/pages/broadcast` for runtime `.vue/.js/.scss` coverage and core portrait/landscape stage class/id preservation. All 77 H5 runtime files still have Mini Program counterparts; Mini extras are platform adapters (`utils/domain-ready.js`, `utils/live-route-context.js`, `utils/live-player-status.js`). `LivePortraitStage.vue` and `LiveLandscapeStage.vue` preserve the key H5 visual anchors (`live-page live-portrait`, `live-landscape`, `landscape-video-stage`, `liveVideo`, `live-video`, `replay-video`, `goodsPic`) while adding only Mini Program-native playback/error/context adaptations. Class/id set scan found no obvious core visual class loss; no safe tiny source patch was identified. Ran only static `rg`, `diff`, file-coverage Node scripts, and `node --check uniapp-src/src/pages/broadcast/utils/live-player-status.js`. Remaining verification boundary: screenshot parity and real DevTools/device playback for live portrait, live landscape, replay portrait, and replay landscape are still required.

- 2026-06-03 19:08 H5 broadcast visual-structure correction: main thread restored H5 text-node structure for portrait/landscape anchor heat and landscape viewer count in `LivePortraitStage.vue` and `LiveLandscapeStage.vue`, removing MP-only `anchor-likes__fire`, `anchor-likes__text`, `viewer-num__fire`, and `viewer-num__text` nodes. Fire icons now use the H5 `::before` selectors in `entry-live.scss` and `entry-landscape-live.scss`, and go-shopping notice product images preserve the H5 `id="goodsPic"` while keeping class compatibility for WXSS. Verification: source scan now reports none of the MP-only visual wrapper classes; H5 class/id set scan has no missing portrait/landscape stage tokens, with remaining extras limited to Mini Program-native live-player/playback-error adaptations and goodsPic compatibility. `npm run build:mp-weixin` passed, `npm run dev:mp-weixin` refreshed `dist/dev/mp-weixin` and exited by timeout, and no current-workspace watch process remains. Remaining verification boundary: WeChat DevTools and real-device screenshot/playback checks for live portrait, live landscape, replay portrait, and replay landscape are still required before declaring pixel-level/full functional completion.

- 2026-06-04 00:?? LiveChatBar H5 structure parity and main-thread verification: restored H5 quick-reply popover text structure and `::after` arrow in `LiveChatBar.vue`, removed MP-only `quick-reply-popover__text`, `quick-reply-popover__arrow`, `tool-btn-center`, and `tool-btn-product` nodes/classes while keeping `quick-reply-eye--active` only as a Mini Program selector-query positioning adapter. Restored live portrait/landscape toolbar background selectors to H5 `toolbar-icons > .tool-btn:nth-child(1)`. Verification: node --check passed for live comments/api/message-channel/websocket files; H5-vs-MP class scan now reports no missing tokens for LiveChatBar, LivePortraitStage, or LiveLandscapeStage, with remaining extras limited to Mini Program-native live-player/playback-error/goodsPic/selector-query adapters. `build:mp-weixin` passed after pinning PATH to nvm Node v22.21.0 to avoid the Homebrew Node icu4c signing issue; `dev:mp-weixin` refreshed `dist/dev/mp-weixin`. Remaining verification boundary: DevTools/real-device playback and interaction validation still required before declaring complete.
